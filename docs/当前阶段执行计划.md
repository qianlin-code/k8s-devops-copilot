# v1.0 求职发布收口

本文档只维护当前阶段的执行顺序、唯一活动子阶段和停止条件。指标与失败案例以[评测与失败案例](评测与失败案例.md)为准，数据许可以[数据来源](数据来源.md)为准，发布状态和门槛以[项目验收标准](../项目验收标准.md)为准。

## 阶段控制

- schema: sliver-stage/v2
- stage_id: ops-v1-release-closeout
- primary_route: 开发执行
- delivery_kind: implementation
- task_depth: D2
- materialization_trigger: [cross_owner_drift, ordered_nonclosable_transition, durable_handoff_requested]
- risk_lanes: [external_provider_effect, identity_permission, license_legal_content, privacy_secret_data, public_contract_compatibility, resource_reliability, supply_chain_config]
- evidence_mode: test
- test_level: T2
- route_evidence_kind: null
- effect_class: local_reversible
- operational_mode: waiting_authorization
- scope_authorization: consumed:生成器修复、最终候选 Git 边界、nanoid 漏洞修复、签名提交、独立干净克隆复验与必要文档回写；不允许付费模型、DeepSeek V4 Flash、Docker、push 或 GitHub 写入
- authorization_substage: 生成器修复后的最终 Git 边界与干净克隆复验
- product_decision: not_required:项目定位与 v1.0 发布门槛已在 README 和项目验收标准中确定
- active_substage: Docker 最终候选隔离复验授权等待
- result_status: signed_candidate_clean_clone_green_docker_wait
- truth_writeback: complete
- migration_state: not_applicable
- evidence_refs: [README.md, AGENTS.md, 项目验收标准.md, docs/架构设计.md, docs/评测与失败案例.md, docs/数据来源.md]

## 阶段目标与用户流程

目标是把已经可运行的 K8s / DevOps 智能运维 Copilot 收敛为可在 GitHub 展示、可由招聘者理解、可从干净克隆复现且不夸大能力的 v1.0 求职项目。

最终可见流程是：读者克隆指定提交，按照 README 配置模型并启动 Compose，初始化演示用户和 7 份 K8s 知识文档，完成知识问答、只读查询、受控写操作、信息不足追问和知识沉淀演示，查看引用与 trace，再通过评测报告、失败案例、CI 和 Release 核对项目质量。集群状态继续使用可复现 mock，不接入生产 Kubernetes。

## 当前真相与 Owner

- 历史基线 commit `31d6551` 的独立干净克隆与隔离 Docker 证据继续保留，但不再作为当前实现候选。当前签名实现候选为 commit `376e38f5e2490f29a78127c3fc82aa863d5c2c10`、tree `753c7c4c69f01e27ab1123712612ba5a9b13e6f9`，父提交是签名实现冻结提交 `947007d7453adb36ba38ca9c6d65626dbb9708c5`；两个提交均由 Ed25519 指纹 `SHA256:pw6HtxZ3a2ldJsvwSISWcmq3t4CDgkhxh2Dp1MCVBkI` 验证通过。相对 `31d6551` 的累计边界为 `67 modified + 12 added = 79`，无删除。
- 基线数据为 7 份 K8s 语料、50 个有效 chunk；来源、CC BY 4.0 说明、获取日期和 SHA-256 由 `docs/数据来源.md` 唯一维护。
- 2026-08-12 已从 commit `31d6551` 的独立临时克隆创建 `copilot-31d6551-e2e-0812` 隔离 Compose 项目（backend `18010`、frontend `15180`），不复用开发数据库或历史验收卷。双容器 healthy，容器内初始化 7 文档/50 chunk，BGE Reranker 已实际启用；脱敏 HTTP/JWT 12 项、容器内 E2E、SSE、真实检索、阈值评测及补装前端依赖后的 Playwright 5/5 均取得 exit 0。证据见 `backend/acceptance-evidence/20260812T132626Z-31d6551-isolated-e2e/`。
- 当前签名候选已收口写参数来源、单主题诊断、工具确定性摘要、裁判上下文、q35 字段约束提示和 API 样例生成器可重复性。DeepSeek V4 Pro + BGE-M3 + BGE Reranker 的固定 39 条严格门禁仍为 `39/39`，知识/只读/写确认路由与引用机器指标均为 100%，q01-q20 及全部工具/写操作人工证据映射复核通过；Qwen-Max v2 的 `0.9821/0.9103` 与原始分歧只作参考。
- commit `376e38f` 的独立 `--no-hardlinks` 克隆位于 `%LOCALAPPDATA%\Temp\k8s-copilot-clean-clone-20260814T080915Z-376e38f5`。Python 3.11.15 / uv 0.12.2 锁定安装后，全量 `296 passed`、契约 `70 passed`；25 个 API 语义快照、前端类型生成、typecheck/build、安全与可观测性检查、整仓 215/215 密钥扫描、npm ci 和 npm audit（0 vulnerabilities）全部通过，最终 HEAD/tree 与 tracked 状态无漂移。本次复验没有模型请求或 Docker 调用。
- 第一版签名候选 `947007d` 的克隆因 `nanoid 3.3.17` 对应 `GHSA-2v37-7h3g-55p8` 停止；授权的 lockfile-only 命令随后未产生变更并按停止条件保留现场。最终通过 `package.json` override 与 lockfile 将传递依赖锁定为 `3.3.18`，因此补丁相对 `947007d` 修改 2 个文件、累计边界从 77 路径扩展为 79 路径。生成器修复前 3 次导出曾意外进入 Qwen 沉淀质量筛选，未输出密钥且修复后的干净克隆为零 Provider 调用；事故证据保留，不将其改写为离线运行。
- 业务范围和对外限制由 `README.md` 管理；架构 owner 是 `docs/架构设计.md`；指标 owner 是 `docs/评测与失败案例.md`；发布状态 owner 是 `项目验收标准.md`。
- 代码 owner 按链路划分：RAG 在 `backend/app/rag/`，Agent/工具在 `backend/app/agent/` 与 `backend/app/services/chat_service.py`，鉴权与隔离在安全依赖和服务层，前端契约由后端 Schema 与类型生成流程共同约束，部署由 `docker-compose.yml` 和初始化脚本约束。
- README、Prompt、前端临时状态、mock 数据和本阶段计划都不得成为业务规则、指标或发布状态的第二 owner。

## 调研决策

- research_status: not_required:本阶段不改变技术栈、架构或外部提供方，只按既有验收标准收口现有实现

本阶段优先使用当前仓库的脚本、CI 配置和 Docker 形态，不引入新的 Agent 框架、向量数据库、鉴权模型或部署拓扑。只有现有路径无法满足已确定验收门槛且有失败证据时，才单独提出基础变更决策。

## 范围与非目标

本阶段范围：固定候选基线；复测生成、路由、引用和代表性失败；验证 Docker 干净克隆；完成远端 CI、演示材料、仓库卫生和 Release 准备；回写最终验收与简历口径。

本阶段非目标：接入真实生产集群、执行任意 shell/kubectl、改造成多集群平台、建设生产级 RBAC/高可用、增加与发布无关的新功能、为了指标删除失败案例或缩小评测集。

## 子阶段计划

| 子阶段 | 结果 | Owner | 完成标准 | 验证 | 不触碰 |
| --- | --- | --- | --- | --- | --- |
| 固定可复现基线 | 选定唯一候选 commit、环境、数据和模型组合，消除工作树与证据混用 | Git 边界、锁文件、配置模板、数据版本、验收基线 | 记录 commit、Python/Node、模型、数据哈希；后端离线测试、前端类型/构建和现有关键 E2E 新鲜通过 | 锁定依赖安装；pytest；typecheck/build；隔离 E2E；仓库卫生扫描 | 不发布、不改远端、不运行付费评测、不删除运行数据 |
| 生成路由引用复测 | 得到可追溯的当前指标、逐条输出和失败案例 | 评测脚本、评测集、Prompt/Agent/RAG owner、评测文档 | 使用同一基线运行路由、生成、引用和人工抽查；完整记录配置、失败和多次运行，不只保留最好结果 | 评测脚本退出码、结果 JSON、失败案例、引用人工抽查 | 不用历史数字冒充当前结果，不硬编码答案，不擅自调用付费模型 |
| Docker 干净克隆验收 | 从新目录完成构建、初始化、核心演示和持久化验证 | README、Compose、镜像、初始化脚本、模型配置 | 按 README 从干净克隆启动；健康、初始化、问答、工具、SSE、引用、重启后读取全部有证据 | `docker compose config/up`、健康请求、初始化日志、Playwright/API 证据、重启检查 | 不复用日常数据库/卷，不停止其他项目，不删除既有 Docker 卷 |
| GitHub 发布准备 | 远端 CI、演示材料和仓库合规达到发布门槛 | GitHub workflow、README、LICENSE、数据清单、演示资产 | 远端 CI 通过；截图/录屏可复现；仓库无密钥与运行产物；Release 草稿完整 | 远端 Actions、敏感信息扫描、仓库体积和链接检查、演示材料复核 | 未单独授权前不 push、不创建 Release、不公开内部证据 |
| 最终验收与简历口径 | 验收结论和对外描述只陈述有证据的能力 | 项目验收标准、评测文档、README、简历草稿 | 必过项全部通过才标记完成；否则保留未验证/不通过和阻塞项 | 验收矩阵、证据链接、最终 SHA/数据/模型、失败案例抽查 | 不把加分项当必过，不宣称生产集群或企业生产落地 |

## 测试、安全与影响

- 阶段整体涉及后端、前端、模型、数据、Docker 和发布多个 owner，实施任务默认按行为决定 T1/T2/T3，不能用阶段的 T2 替代每个改动自己的测试门禁分类。
- 稳定行为或 BUG 缺少现有门禁时先建立 RED，再修改 owner 并观察 GREEN；真实模型波动、Docker/浏览器/外部服务使用可重复 T3 路径并保留环境证据。
- JWT、admin、跨用户隔离、写操作确认/拒绝/幂等、提示词注入、日志脱敏和资源上限必须保留允许与拒绝的配对测试。
- 真实模型与 Docker 运行必须使用隔离端口、数据库/卷和明确停止条件；不得自动接管其他项目进程或把 mock-backed E2E 当成真实模型质量证据。
- 发布前检查密钥、个人路径、日志、数据库、Qdrant、模型缓存、测试产物和数据许可；公开动作必须重新取得精确仓库和分支授权。

## 验证方法

1. 基线：在选定 commit 的干净目录按锁文件安装，执行后端完整 pytest、前端 typecheck/build 和当前关键 E2E，记录测试发现数量与退出码。
2. 评测：使用固定数据版本、模型和参数运行检索/路由/生成/引用评测，保存逐条结果与失败案例；达到门槛与否只更新评测和验收 owner。
3. 部署：使用独立 Compose project、端口和卷从干净克隆执行配置、构建、初始化、API/浏览器演示、停止与重启持久化检查。
4. 发布：核对远端 CI、公开数据许可、敏感信息、仓库体积、Markdown 链接、截图/录屏和 Release 内容。
5. 收口：执行项目验收标准的全部必过项；Skill 阶段护栏结构通过不代表运行或发布验收通过。

## 停止条件与未验证

- 本次授权已用于生成器修复、候选冻结、nanoid 漏洞修复、两个 SSH 签名实现提交、独立干净克隆复验和文档回写。最终复验没有运行 Qwen-Max、DeepSeek 或其他付费模型，也没有运行 Docker、push、GitHub、远端 CI、录屏、tag 或 Release。
- 候选工作树与已有 commit 的行为或数据不一致时停止，先选定唯一基线，不把两者证据拼接。
- 评测数据、模型或参数无法固定，付费模型需要凭据/成本授权，或真实服务正在被其他项目占用时停止对应运行。
- 同一严重 BUG 两次修复未解决时暂停补丁；第三次仍失败时重新审计架构、owner 和真源后才能继续。
- 已解除：旧正式裁判暴露的问题、q35 可操作性缺口、生成器快照漂移以及 `nanoid` 高危审计问题；当前签名候选的最终 SHA 离线干净克隆已通过。未验证：当前候选的隔离 Docker 验收、远端 Actions、演示材料、Release 和最终简历描述。`31d6551` 的历史 Docker 证据不能替代当前候选或生产部署。
- 每个子阶段结束后必须回写新鲜证据并把 `active_substage` 显式推进；不得自动连续执行后续 Git、外部写入或发布动作。

## 实施回写

本节在活动子阶段实际执行并验证后填写。阶段结构通过不等同于项目完成。

### 2026-08-14：生成器修复、候选重新冻结与干净克隆通过

- generator: `export_api_examples.py` 的写操作夹具现逐字包含路由参数，确认流程先校验状态、outcome、pending write、工具名和非空令牌；`--output-dir` 与无写入的 `--check` 使用按文件和 JSON 路径建立的稳定动态值映射完成语义比较。6 项生成器回归通过，最终 25 个 tracked API 样例与前端类型均无漂移。
- provider_incident: 修复前的 3 次完整导出因 `Settings` 从 `backend/.env` 重新加载 Qwen key，且沉淀 client factory 未被替换，意外进入 Qwen 沉淀质量筛选。日志未输出或持久化凭据，具体费用不可从响应确定；修复后将付费 provider 环境值置空、替换独立 client factory 并验证本地 scripted client。事故记录保存在 ignored `acceptance-evidence/20260814T072015Z-git-boundary-clean-clone-v2/unexpected-provider-call-incident.md`，不得把受影响运行描述为离线。
- first_freeze: 原 75 路径加生成器脚本和测试形成 77 路径边界，SSH 签名提交 `947007d7453adb36ba38ca9c6d65626dbb9708c5`、tree `deae77e86f663028d97bd4a9471fd1ed68883351` 创建并验证通过。其独立克隆完成后端、契约、生成物和前端构建，但 npm audit 因 `nanoid 3.3.17` 的 `GHSA-2v37-7h3g-55p8` 返回 1 个 high vulnerability，因此按停止条件保留现场且没有创建文档提交。
- failed_remediation: 授权的一次 `npm.cmd install nanoid@3.3.18 --package-lock-only --ignore-scripts --no-save --no-audit --no-fund` 返回 up to date，`package.json` 和 lockfile 均未变化，实际仍锁定 `3.3.17`。现场保存在 ignored `acceptance-evidence/20260814T080245Z-nanoid-3.3.18-clean-clone/`；没有自动重试、audit、提交或克隆。
- dependency_fix: 采用 npm `overrides.nanoid=3.3.18` 后重新生成 lockfile，解析链为 `vite 6.4.3 -> postcss 8.5.26 -> nanoid 3.3.18 overridden`。签名补丁提交 `376e38f5e2490f29a78127c3fc82aa863d5c2c10`、tree `753c7c4c69f01e27ab1123712612ba5a9b13e6f9` 相对 `947007d` 只修改 `frontend/package.json` 与 `frontend/package-lock.json`；相对 `31d6551` 的累计边界为 `67 modified + 12 added = 79`，无删除。提交由 Ed25519 指纹 `SHA256:pw6HtxZ3a2ldJsvwSISWcmq3t4CDgkhxh2Dp1MCVBkI` 验证通过。
- clean_clone: 在 `%LOCALAPPDATA%\Temp\k8s-copilot-clean-clone-20260814T080915Z-376e38f5` 使用 `git clone --no-hardlinks` 和 detached checkout 复验。Python 3.11.15 / uv 0.12.2 frozen export 与 50 包独立安装通过；前端类型生成无漂移、25 个 API 语义快照通过；后端 `296 passed in 104.39s`、契约 `70 passed in 27.61s`；安全与可观测性通过，整仓密钥扫描 215/215、0 findings。Node 24.18.0 / npm 11.16.0 下 npm ci、单一 `nanoid 3.3.18` 依赖树、typecheck、build 和最终 audit 0 vulnerabilities 全部通过。
- hygiene: 克隆最终 HEAD/tree 未漂移、tracked status 为空且 `git diff --check` 通过；不存在 `.env`、evidence、数据库或日志。`.venv`、`node_modules`、`dist`、pytest/Python cache 与 `tsconfig.tsbuildinfo` 均为预期 ignored 运行产物并保留现场。汇总证据为 ignored `acceptance-evidence/20260814T080534Z-nanoid-override-clean-clone/30-verification-summary.json`。
- decision: 当前实现候选的 Git 边界和离线干净克隆门禁通过，项目仍为“未验证/未发布”。active substage 推进到“Docker 最终候选隔离复验授权等待”；不自动运行 Docker、push、远端 CI、录屏、tag 或 Release。

### 2026-08-14：q35 生产 Schema 约束提示确定性收口

- red_gate: 缺失 description、过短 description、模型补造但用户未提供 description，以及非法 title 四个契约场景均先按预期失败；旧实现只返回裸 `description`/`title` 字段名。
- implementation: Agent 在写参数校验和确认前预校验两个入口统一读取生产 Tool Pydantic JSON Schema。原始字段路径先去重排序，再把 `minLength/maxLength` 渲染为 `description（至少 10 个字符）`、`title（4–120 个字符）`；未知字段退回原名，`request_id` 不向用户展示。没有增加 q35、case ID 或 gold 文本特例。
- safety: 缺失、非法或用户未明确提供的写参数仍直接进入信息不足分支；确认令牌、工具调用和写审计均不会提前产生，也不会自动补造参数。
- green_gate: Python 3.11 定向 `4 passed`、独立契约 `70 passed`、全量 `290 passed`。本次没有修改公共 API、Schema、前端类型、SSE、数据库、Prompt、评测集、gold、阈值或路由逻辑，也没有调用任何付费模型。
- acceptance: DeepSeek V4 Pro 固定 39 条严格门禁、服务端证据映射和人工复核继续作为发布主标准；Qwen-Max 原始报告与分歧不删除，但仅作独立参考。裁判低分只有经生产 Schema、契约或人工证据独立确认后才形成阻塞；不以 DeepSeek 自评替代独立证据。
- configuration: 默认组合保持 DeepSeek V4 Pro + BGE-M3 + BGE Reranker；不接入 DeepSeek V4 Flash，Flash 留待 v1 收口后的独立成本/时延 A/B。
- decision: OPS-BLOCK-07 已解除，项目仍为“未验证/未发布”。active substage 推进到“最终候选 Git 边界与干净克隆授权等待”。
- stop_condition: 不执行 Docker、Git 暂存/提交/推送、GitHub、远端 CI、录屏、tag 或 Release。

### 2026-08-14：Qwen-Max v2 正式复裁聚合通过，q35 分歧复核阻塞发布

- formal_judge: 对 SHA-256 `bb5096eef0391b4a35702d66df73f29a6c10c666f81fa0da694ea4b94c0f0215` 的固定 39 条 DeepSeek 报告运行一次 qwen-max `ragas-judge-v2-verified-evidence`。39/39 次调用成功，Faithfulness `0.982051`、Answer relevancy `0.910256`，超过 `0.70/0.65` 聚合门槛；裁判使用 45248 tokens、费用 `0.904960 元`，连同既有生成费用合计 `1.018691 元`，低于授权的 `20 元`硬上限。正式报告 SHA-256 为 `28a9c4dc823d82e1572106019f2070a26ec98090892daab7bcffb5dd3b73af86`。
- disagreement_review: 低分共 3 条。q24 已在证据步骤 1 逐字给出 gold 所需的 `kubectl auth can-i` 方法，因额外 RBAC 排查内容被降相关性，归为裁判聚焦度分歧；q35b 的 namespace/title/description/priority 已由 `pending_write` 确认卡展示，裁判上下文也包含该卡，归为复合 UI 评分分歧。q35 虽正确追问 namespace/description 且未提前确认或执行，但没有告知 description 至少需要 10 个字符，属于会阻碍用户补齐参数的实质缺陷。
- evidence: ignored `acceptance-evidence/20260814T063012Z-qwen-max-formal-v2/qwen-max-judged.json` 与同目录 `qwen-max-disagreement-review.json`；原始裁判分数不改写。
- decision: 聚合门槛通过不能覆盖 q35 实质缺陷，项目继续保持未发布。active substage 推进到“q35 写操作缺失字段约束提示修复授权等待”；本子阶段不修改实现代码，也不运行第二次付费裁判。
- stop_condition: 不执行 q35 修复、Docker、Git/GitHub、远端 CI、录屏、tag 或 Release。

### 2026-08-14：DeepSeek 发布基准与确定性工具回答门禁通过

- implementation: 写工具参数必须能从当前问题或用户历史精确定位；`create_incident` 缺失或模型补造的 `namespace/title/description` 会一次性合并追问，确认令牌、工具调用和审计均不会提前产生。成功只读工具由服务端生成 `/answer_summary`，Answerer 必须选中该原子；诊断类知识选择受单主题、根因与同主题步骤约束。
- real_generation: 最新报告 `acceptance-evidence/20260814T060050Z-deepseek-39-route-final/deepseek-39-report.json` 为 `39/39`、exit 0、无 fallback；知识路由、只读工具、写确认、引用覆盖和关键词支持均为 100%。49 次 DeepSeek 调用全部成功，费用 `0.113731 元`。q29 仅使用 NetworkPolicy，q35 同时追问 namespace/description，q31/q32/q33/q36/q37/q38 的事实来自对应工具摘要原子。
- manual_review: 从同一报告复核 q01-q20 和全部工具/写操作案例。回答文本、知识原子、工具 Pointer、chunk、标签与最终展示一致；q12 未否定 Waiting，q14 仅使用 DNS，q34/q35b 只确认不执行，q35 不产生确认或调用。
- judge_preflight: 新候选 Qwen-Max 零预算预检 `judge_mode=preflight`、exit 0，未初始化裁判；预计 131820 tokens、裁判 `2.6364 元`，加生成共 `2.750131 元`。Qwen 只作为独立参考裁判，单项低分需回到服务端证据人工复核；该子阶段结束时正式付费复裁仍需单独授权。
- configuration: v1.0 非密钥默认组合调整为 DeepSeek V4 Pro + 本地 BGE-M3 + BGE Reranker；Ollama `qwen2.5:7b` 保留为显式离线兼容模式。未接入 DeepSeek V4 Flash。
- final_offline_gate: Python 3.11 全量 `289 passed`、独立契约 `69 passed`；前端 typecheck/build、密钥与安全扫描、`git diff --check` 均 exit 0。安全报告保存在 ignored `acceptance-evidence/20260814T061759Z-final-security/`。
- stop_condition: 本子阶段当时不运行付费复裁、Docker、Git/GitHub、远端 CI、录屏、tag 或 Release；完成最终离线门禁后等待对应授权。

### 2026-08-14：qwen-max 正式裁判聚合通过，分歧复核阻塞发布

- formal_judge: 对 SHA-256 `ad880329e1e814816aa3546a5ec9adbccef212c48489afbfcacc9ddf465ad0fc` 的 39 条 DeepSeek 生成报告运行 qwen-max `ragas-judge-v1`。39/39 次调用成功，Faithfulness 与 Answer relevancy 均为 `0.8154`，超过 `0.70/0.65` 聚合门槛；裁判费用 `0.985380 元`，生成与裁判合计 `1.037948 元`。正式报告 SHA-256 为 `fc2cf03d1122a6ccfcdce558a19c2be115397dc9771b2af9a6bbb2154b29cc03`。
- disagreement_review: 11 条存在单项低分。q31/q32/q33/q34/q35b/q36 的主要问题是裁判提示只包含知识检索片段，没有包含最终工具 T 原子或 pending_write 卡片，原始低分保留为评测协议分歧。q17/q22/q29/q35/q37 则确认存在实质缺陷：遗漏 gold 关键检查、混入无关主题、漏追问 namespace，或未回答副本为 0 时不能重启。
- audit_scope: 先前 q01-q20 的 `20/20` 只证明展示文本与原子/chunk/标签精确对应，不证明对 gold answer 的覆盖完整；q17 的相关性缺口不推翻映射审计，但会阻塞发布。
- evidence: ignored `acceptance-evidence/20260814T025617Z-deepseek-bge-m3-39/qwen-max-judged.json` 与 `qwen-max-disagreement-review.json`。
- stop_condition: 不切换 DeepSeek 为发布默认，不运行通过后离线收口或任何 Docker/Git/GitHub/CI/tag/Release 动作。下一子阶段必须把“工具/确认上下文裁判协议”与“五条候选行为缺陷”分开设计和验证，不得用聚合平均分掩盖单项实质错误。

### 2026-08-14：工具证据协议与 DeepSeek/BGE-M3 生成门禁通过

- actual_result: Answerer 现要求存在成功工具调用时，最终证据至少包含一个对应的 `T#.A#` 原子；首次遗漏返回结构化 `missing_tool_evidence`，第二次只展示成功工具结果原子并保留原 ID。第二次非法、空、重复或选择被排除原子时确定性降级，不产生第三次调用。失败工具和调用参数仍不可见；无成功工具时知识回答行为不变。
- offline_gate: Python 3.11 Answerer/状态机定向回归 `82 passed`、全量 `268 passed`、独立契约 `63 passed`；前端 typecheck/build、密钥扫描与 `git diff --check` 均通过。本轮未修改公开 Schema、SSE、数据库、前端 API 类型、语料、评测 case、gold answer、关键词或阈值。
- real_generation: 发布组合显式锁定 DeepSeek V4 Pro、`bge-m3`（1024 维）与 BGE Reranker。q36 为 `1/1`、verified、无 fallback，工具状态与重启次数来自 `T1` 原子，费用 `0.001670 元`；固定 13 条为 `13/13`、全部 verified、引用覆盖/关键词支持 100%，费用 `0.015210 元`；完整 39 条为 `39/39`、知识/只读/写确认路由及引用两项均 100%，exit 0，46 次成功生成调用，费用 `0.052568 元`。
- manual_review: 从同一份完整报告固定审核 q01-q20，`20/20` 的回答文本、证据原子、chunk 与引用标签逐项一致。q03 无虚构 CrashLoopBackOff 时长，q12 正确区分 Pending/Waiting，q14 仅使用 DNS 根因与步骤，q19 不承诺传播时延。q10/q18 的来源许可尾注被完整吸收到步骤原子，内容真实但展示冗余，登记为非阻塞展示问题。
- judge_preflight: qwen-max `ragas-judge-v1` 零预算预检 exit 0，未初始化或调用裁判；估算裁判 token 上界 `135778`、裁判费用 `2.715560 元`，连同生成费用合计 `2.768128 元`。正式 Faithfulness/Answer relevancy 仍为 `not_run`。
- evidence: ignored `acceptance-evidence/20260814T025245Z-deepseek-bge-m3-q36/`、`acceptance-evidence/20260814T025338Z-deepseek-bge-m3-13/`、`acceptance-evidence/20260814T025617Z-deepseek-bge-m3-39/`。完整报告 SHA-256 为 `ad880329e1e814816aa3546a5ec9adbccef212c48489afbfcacc9ddf465ad0fc`，eval SHA-256 为 `f061f7db826a3aa28fac995bd9d28bb11db5c126e97db1ec9063a2679c11d79d`，脚本 SHA-256 为 `759d3bfb9ad4782aea2e161cfe46766b3ef4193af127fb15d73aeba2367e546d`。
- stop_condition: 正式 qwen-max 裁判需要单独费用授权；DeepSeek 尚未切换为发布默认模型。Docker、Git/GitHub、远端 CI、录屏、tag 和 Release 均未执行，项目继续保持“未验证/未发布”。

### 2026-08-14：Markdown 列表完整性修复通过，完整生成门禁阻塞于 q36

- actual_result: `MarkdownHeaderChunker` 对程序性小节改为按完整顶层列表项打包；续行、说明和 fenced code block 保留在所属列表项，单个超长列表项使用软大小上限且不再字符截断。非程序性小节和无列表边界的小节继续使用原字符分块。q09/q14 真实语料截断、编号/无序列表、续行、代码块、稳定顺序和超长单项均已加入回归。
- offline_gate: Python 3.11 全量 `261 passed`、独立契约 `63 passed`；前端 typecheck/build 和 `git diff --check` 均 exit 0。本轮未修改公开 Schema、前端生成类型、语料、评测 case、gold answer、关键词或检索阈值。
- real_generation: DeepSeek V4 Pro + 本地 BGE 的 q09/q14 为 `2/2`，一次生成、无 fallback，费用 `0.005558 元`；固定 13 条为 `13/13`，全部 verified、引用覆盖/关键词支持 100%，费用 `0.024849 元`。新分块后的完整 39 条为 `38/39`，知识/只读/写确认路由汇总及引用指标均为 100%，但 q36 被严格门禁拒绝。
- blocking_result: q36 正确调用成功的 `get_pod_status` 并得到 `tool_assisted_answer`，但最终 Verified Answer 只选择知识 K 原子，没有选择任何成功工具 T 原子，失败码为 `missing_verified_tool_evidence`。报告 exit 2；这不是 Markdown 截断或检索失败，不能通过重跑、旧报告或放宽门禁登记为通过。
- evidence: ignored `acceptance-evidence/20260814T015607Z-deepseek-q09-q14-chunker/`、`acceptance-evidence/20260814T020007Z-deepseek-13-chunker/`、`acceptance-evidence/20260814T020402Z-deepseek-39-chunker/`。完整报告固定 eval SHA-256 `f061f7db826a3aa28fac995bd9d28bb11db5c126e97db1ec9063a2679c11d79d` 与脚本 SHA-256 `759d3bfb9ad4782aea2e161cfe46766b3ef4193af127fb15d73aeba2367e546d`。
- stop_condition: 未开始 q01-q20 最终人工复核或 qwen-max 零预算预检；未运行正式裁判、Docker、Git/GitHub、CI、tag 或 Release。下一步需单独设计“成功工具结果必须进入工具辅助回答最终证据”的通用协议和 RED 回归，不得添加 q36/case 文本特例。

### 2026-08-13：DeepSeek V4 Pro A/B 离线实现完成，q29 未启动

- actual_result: 新增官方 OpenAI 兼容 `deepseek` Chat provider，默认候选模型为 `deepseek-v4-pro`；Embedding/Reranker 仍固定为本地 BGE。评测 CLI 支持 `--mode deepseek --no-judge`，报告记录 provider、模型、请求数、输入缓存命中/未命中 token、输出 token、分项价格、费用和预算。费用预检按未缓存输入 `0.003 元/千 token` 与输出 `0.006 元/千 token` 保守预留，响应含缓存明细时按 `0.000025 元/千 token` 结算。
- evaluation_contract: q34/q37 正向问题已显式包含 `ops-demo`，gold answer、期望工具、关键词、语料和检索阈值未修改。新增缺 namespace 的读写负控；即使模型自行填入未在用户问题或用户历史中出现的 namespace，服务端也会在工具调用、确认令牌和审计之前拒绝并追问。
- offline_gate: Python 3.11 全量 `248 passed`、独立契约 `63 passed`；前端 typecheck/build exit 0；`git diff --check` exit 0；171 个 tracked/unignored 源文件密钥扫描 0 命中。DeepSeek provider、缺 Key、分项费用、缓存计费、预算前置拒绝、报告驱动裁判兼容和密钥脱敏定向门禁均通过。
- blocking_result: 被 Git 忽略的 `backend/.env` 存在，但 `DEEPSEEK_API_KEY` 为空。未读取或输出任何 Key，未发起 DeepSeek 请求，0.5 元 q29 A/B 尚未运行。
- next_substage: 用户仅在本地 `backend/.env` 填入有效 `DEEPSEEK_API_KEY` 后，以 0.5 元硬上限运行 q29；未通过则立即停止，不运行固定 13 条或完整 39 条。
- git_checkpoint: 工作树保持未暂存、未提交、未推送；DeepSeek 尚未成为发布默认模型。

- actual_result: q19 已按授权重新界定 gold answer 与评测关键词：只要求 Kubernetes API 动态配置、RoleBinding/ClusterRoleBinding 授权和 `kubectl auth can-i` 验证，不再要求“即时生效/无需重启”。新增官方语义边界段落后，q19 直接证据定位测试 GREEN；定向回归 48 passed，输出目录契约 16 passed，最终 Python 3.11 全量回归 169 passed。真实 BGE 阈值复测 39/39、BGEReranker 实际启用、7 文档/51 chunk；无付费严格门禁 exit 0、39/39，引用覆盖/关键词支撑 100%，未调用 Qwen。固定 q01-q20 新工程复核为 18/20（90.0%），q19 已支持；q03 仍混入 ImagePullBackOff 退避时长，q12 仍输出“Waiting 不是 Kubernetes 状态”，两题保留未通过。
- changed_owners: `backend/app/rag/query_policy.py`、`backend/app/config.py`、`backend/app/services/chat_service.py`、`backend/app/agent/state_machine.py`、`backend/scripts/eval_ragas.py`、阈值评测脚本与测试；阶段、评测和验收文档回写。本轮新增 ignored 工程审阅与本地发布前静态核查 evidence，未修改产品代码、Prompt、语料、评测集、Schema、Compose 或生产全局阈值。
- plan_deviation: 新增 `eval_ragas.py --no-judge` 后，历史 38 条严格评测以 exit 2（13/38 严格通过）触发停止条件，未运行付费 Qwen 裁判。本轮先在同一隔离现场补跑 q03、q19、q35、q35b、q36 五条诊断探针：exit 2、3/5 通过；q35/q35b/q36 已通过，q03/q19 仍因 Rerank 阈值过滤后无知识上下文而失败。首次编排未先执行 `npm ci`，因此 Playwright 的首次失败只记录为编排缺口，未掩盖且已在同一隔离现场补救验证。
- fresh_evidence: `backend/acceptance-evidence/20260812T144000Z-q19-redefined-rerank-threshold-39/`、`20260812T144000Z-q19-redefined-no-judge/` 和 `20260812T144000Z-q19-redefined-citation-review/`；另记录输出目录创建修复的 16 项定向测试与旧口径 39/39 但未落盘的失败现场。
- remaining_risk: q03/q12 真实生成引用仍未达到无误支持；固定 q01-q20 仅达到 90.0% 门槛，不等同三题全部修复。Qwen Faithfulness/Answer relevancy、远端 CI、演示录屏、Release 和最终简历描述仍未验证；全局阈值 `0.12` 与知识阈值 `0.03` 均未改变。
- next_substage: q03/q12 生成约束 owner 的独立 RED→GREEN 修复；不得借评测集、case ID、全局阈值或 Prompt 特例追指标。
- git_checkpoint: 本轮不暂存、不提交、不推送。

### 2026-08-12：q03/q12 Answerer 证据一致性收口（代码 GREEN，真实复测阻塞）

- actual_result: 在 `backend/app/agent/answerer.py` 增加基于当前问题与检索片段的通用后处理，不读取评测集或 case ID。知识-only 的 CrashLoopBackOff 时间问题会移除 ImagePullBackOff 的 300 秒/5 分钟误引，并保留退避逐次拉长、没有确定恢复时长与 `kubectl logs <POD> --previous`；Pending/Waiting 问题会保留 Waiting 语义并区分 Pod 调度阶段与容器启动阶段。工具结果与空上下文不进入该后处理。
- test_gate: T2 RED→GREEN；新增 Answerer 约束测试先观察失败，修复后定向 `7 passed`，相关契约合计 `30 passed`。系统 Python 3.13 全量回归 `172 passed`，仅作 fallback，不替代项目要求的 Python 3.11。
- fresh_evidence: `backend/acceptance-evidence/20260812T-q03-q12-answerer-evidence-closeout/answerer-constraint-closeout.json`。
- blocking_result: `backend/.venv/Scripts/python.exe` 为 uv trampoline，启动 Python 3.11 时返回 `permission denied (os error 5)`；因此停止真实 BGE 39 条阈值评测、`eval_ragas.py --no-judge` 与 q01-q20 新引用复核，不把 Python 3.13 结果写成 3.11 证据。
- remaining_risk: 真实模型输出是否已完全消除 q03/q12 误引尚未验证；q19 新口径、全局阈值 `0.12`、知识阈值 `0.03` 均未改变。Qwen、Docker、Git/GitHub、Faithfulness、Answer relevancy、远端 CI、演示与 Release 仍未验证。
- next_substage: 恢复可执行的 Python 3.11 虚拟环境后，重新运行固定 39 条 BGE、无付费严格门禁和 q01-q20 工程引用复核；任一失败则停止并如实登记。

### 2026-08-13：Python 3.11 隔离环境恢复复核仍阻塞

- environment_result: 新建 `backend/.venv-recheck311`，确认目标解释器为 Python 3.11.15，并完成项目 dev/rerank 依赖安装；为绕过中文路径 `.pth` 的 GBK 启动问题，仅修改了该新环境内的 editable `.pth`，未修改仓库代码或现有 `.venv`。
- blocking_result: 新环境的 uv trampoline 仍无法在当前沙箱启动子解释器（`permission denied`, os error 5）；`uv run` 也因无法读取用户 uv Python 目录被阻断。受控执行申请因本地审批代理额度不足被拒，未绕过该边界。
- fresh_evidence: `backend/acceptance-evidence/20260812T-q03-q12-answerer-evidence-closeout/python311-environment-recheck.json`。
- status: Python 3.11 测试、BGE 39 条阈值评测、`eval_ragas.py --no-judge` 和 q01-q20 引用复核仍未运行；代码 GREEN 与 Python 3.13 fallback 结果不替代真实模型证据。

### 2026-08-13：最终无付费门禁 GREEN，人工引用复核 RED

- actual_result: 受控 Python 3.11 执行已恢复；最终后端 `187 passed`，前端 typecheck/build exit 0。真实 Ollama/BGE 固定 39 条无付费门禁 `39/39`，五项机器指标均 100%。
- manual_review: q03/q12 以及 q09/q13/q14/q19 的已知错误消除，但 q02/q04/q05/q07/q16/q17/q18 仍有证据无法逐句定位的命令、写操作建议或扩展结论，人工复核不通过。
- stop_condition: 同一 Answerer owner 连续两轮后处理修复仍出现新的逐句忠实度失败，停止继续添加 case 模板，下一子阶段改为审计可验证引用生成机制。
- fresh_evidence: `backend/acceptance-evidence/20260813T085033Z-v1-no-judge-grounded-final/`。
- remaining_risk: Qwen Faithfulness/Answer relevancy、Docker 最终候选复验、Git 提交、远端 CI、演示材料和 Release 均未执行；项目保持“未验证/未发布”。

### 2026-08-13：Verified Answer 机制落地，真实定向门禁 RED

- actual_result: 已移除 q03/q12 式自由文本后处理，知识与成功工具结果统一进入 `VerifiedAnswerPlan`。模型只能选择 K# 精确原文或 T# RFC 6901 Pointer，服务端验证并确定性渲染；两次结构化/语义校验失败或模型不可用时安全降级为 `insufficient_information`，不回退自由文本生成。所有生成回答出口统一记录 `answer_evidence` 与 `answer_generation`，前端 Agent trace 可查看逐项映射，历史 trace 兼容空字段。
- evaluation_contract: `eval_ragas.py` 的严格引用门禁已改为只审计最终 verified mapping；检索到但未进入答案的 chunk 只能作为诊断，不能满足关键词支持。工具辅助回答必须含对应 T 映射，fallback 不能算严格通过；q01-q20 人工清单输出最终回答、生成状态和逐项 K/T 映射。
- offline_gate: Python 3.11 全量 `200 passed, 16 skipped`、exit 0；前端 typecheck/build 均 exit 0。16 个 skip 是已由 verified-plan 覆盖的旧自由文本后处理测试，未作为通过项隐藏。Schema 类型和 API 样例已重新生成；接口响应/SSE/数据库结构未迁移。
- real_gate: 首轮真实 Ollama `qwen2.5:7b` + BGE-M3 + BGE Reranker 定向 13 条为 `0/13`。通用 discriminator Schema、verbatim 边界、受控修复提示和 section 校验迭代后，q02/q03 最终定向仍为 `0/2`、exit 2；两条均安全降级，`fallback_reason=no_evidence_selected`。中间 q02 曾以精确原文通过，但把程序性片段放入结论，服务端新增通用 section 约束后不再放行。
- stop_condition: 已连续多轮以同两条真实 case 调整通用 Prompt，最终仍失败；为避免演变为隐性评测特例，停止继续调 Prompt。未运行完整 39 条、q01-q20 人工复核、费用上限 0 的 Qwen 预检、Docker、Git 或发布动作。
- fresh_evidence: ignored `acceptance-evidence/20260813T-verified-answer-targeted/`、`20260813T-verified-answer-diagnose-q02-q03/`、`20260813T-verified-answer-schema-q02-q03/`、`20260813T-verified-answer-verbatim-q02-q03/`、`20260813T-verified-answer-sections-q02-q03/`。所有失败现场保留，未覆盖为最好一轮。
- next_substage: 重新审计本地 7B 承担“逐字抽取”任务的可行性与 owner 边界；在新的通用方案和独立 RED 证据形成前，不继续全量评测或发布推进。
- git_checkpoint: 工作树保持未暂存、未提交、未推送。
