# 运维 Copilot 评测与失败案例

本文档是本项目指标、实验条件和失败案例的唯一事实来源。README 只引用摘要；`项目验收标准.md` 定义门槛，不复制实验细节。

## 1. 结果使用规则

每次正式评测必须记录：日期、commit SHA、数据集哈希与样本数、知识/工具样本划分、LLM/Embedding/Rerank 模型、关键参数、硬件、运行命令、总耗时和逐条结果。

以下历史数字来自场景迁移期间的记录。JWT、Embedding 默认值和关联召回后来发生过变化，因此在当前提交上复测前只能称为“历史基线”，不能称为最新结果。

## 2. 评测体系

### 2.1 检索

`backend/scripts/eval_retrieval.py` 比较三条链路：纯向量、向量+BM25+RRF、完整链路+Rerank。候选召回宽度必须大于最终 Top-K，否则 Rerank 只能换顺序，无法改变 Hit@K 集合。

主要指标：Hit@1/3/5、MRR、命中片段平均排名和延迟。知识问题与需要工具实时状态的问题分开统计。

### 2.2 路由与生成

`backend/scripts/eval_ragas.py` 使用人工标注关键词计算 context precision/recall，并用独立云端裁判计算 faithfulness 和 answer relevancy。

路由拆为两个指标：

- `knowledge_routing_accuracy`：知识问题没有被误触发成工具操作。
- `tool_routing_accuracy`：工具问题触发了正确工具。

两者不能合并。一个模型可能减少误触发，却把答案改成过度保守拒答，路由数字上升而回答质量不变。

### 2.3 阈值

`eval_rerank_threshold.py` 比较不同 `min_rerank_score` 下的命中率、空 context 和噪声。阈值调整后必须再跑路由与生成评测，不能只优化检索层。

## 3. 历史基线（待当前提交复测）

### 3.1 K8s 检索历史结果

历史评测集包含 38 条案例，其中知识检索指标来自知识类样本；当时语料为 7 篇 K8s 文档、约 50 个 chunk。

| 配置 | Hit@1 | Hit@3 | Hit@5 | MRR | 说明 |
| --- | ---: | ---: | ---: | ---: | --- |
| 纯向量 | 39.5% | 55.3% | 57.9% | 0.471 | 历史基线 |
| 向量 + BM25 + RRF | 57.9% | 78.9% | 84.2% | 0.693 | 关键词补充专有名词召回 |
| 完整链路 + Rerank | 78.9% | 86.8% | 86.8% | 0.829 | 历史最佳记录 |

17 条口语化 hard 子集的历史记录：纯向量 Hit@3 58.8% / MRR 0.520，混合检索 88.2% / 0.779，完整链路 100% / 0.941。

### 3.2 路由与生成历史结果

| 指标 | 历史记录 | 限制 |
| --- | ---: | --- |
| context precision | 57.4% | 需在当前检索配置复测 |
| context recall | 68.9% | 同上 |
| knowledge routing accuracy | 86.7%（26/30） | 后续 Prompt/关联召回实验曾出现明显回归 |
| tool routing accuracy | 100%（8/8） | 样本量较小 |
| faithfulness | 0.718 | 裁判模型与 Prompt 必须固定 |
| answer relevancy | 0.691 | 同上 |

旧阶段验证文档曾记录 JWT 改造后 131 项测试中仍有 19 项失败。该记录只说明当时状态；2026-08-11 的 Python 3.11 离线全量 pytest 已通过 137 项，历史失败不再是当前阻塞。真实模型与服务联调仍需单独验收。

## 4. 代表性失败案例

### 4.1 三阶段文本表示不一致

曾出现向量和 BM25 使用“标题链+正文”，Rerank 只看裸正文的实现。正确根因段落因缺少问题关键词被 Rerank 从前列压到后排，Rerank 反而降低 Hit@5。

结论：Embedding、BM25、Rerank 必须共用 `contextual_text`；由回归测试锁定这一隐式契约。

### 4.2 召回宽度等于输出宽度

早期评测召回 5 条再输出 5 条，Rerank 只能重排同一集合，Hit@5 必然不变，造成“Rerank 无效”的错误判断。

结论：召回候选必须显著大于 Top-K，并同时看 MRR/平均排名和 Hit@K。

### 4.3 评测集过于简单

12 条查询时混合检索已接近满分，任何优化都难以体现。加入口语化、易混淆和无直接术语的 hard case 后，才观察到双路召回与 Rerank 的真实差异。

结论：评测集要有主题区分度、难例和失败分支，不能只用 README 示例问题。

### 4.4 本地 7B 路由过度行动

历史完整评测中，部分知识问题被路由到工具分支。单纯在 Prompt 中强调“不要调用工具”曾把一类错误换成过度拒答，且误伤原本正确的工具案例。

结论：知识路由与工具路由分别报告；Prompt 修改必须对完整评测集回归，不能只修目标 case。

### 4.5 生成阶段编造运行事实

曾出现模型在没有工具结果支持时编造 Deployment 状态或 kubectl 命令。检索命中和路由正确不能保证最终回答忠实。

结论：工具结果逐字引用，回答前保留充分性校验，并独立衡量 faithfulness。

### 4.6 Rerank 阈值的局部最优

历史敏感性实验把阈值从 0.15 调至 0.12，减少空 context 并改善 hard case；继续降低到 0.10/0.08 不再增加命中，只引入噪声。

结论：0.12 是特定语料和模型下的历史选择，不是通用常量；更换 Embedding、Rerank 或语料后必须重跑。

### 4.7 Chunk 元数据提示造成回归

为“处理步骤”chunk 增加路由降权提示的实验只修复了少数案例，却让知识路由整体明显下降。这说明局部合理的规则可能改变模型对整批证据的理解。

结论：元数据只作为辅助；保留关联召回等结构性方案，但任何上线结论以完整评测为准。

### 4.8 幂等键全局唯一导致串会话

模型生成的 `request_id` 可能碰撞。若数据库只按 request_id 全局查找，会话 B 可能拿到会话 A 的写操作结果。

结论：幂等键限定为 `(conversation_id, request_id)`；此类安全回归必须由测试覆盖。

## 5. 当前正式结果

本次公开发布准备不执行付费或真实模型评测。完成下一次全量评测后，在此登记并同步 README 摘要：

| 字段 | 值 |
| --- | --- |
| 日期 / commit | 待复测 |
| 数据集版本与样本数 | 待复测 |
| 模型与参数 | 待复测 |
| 检索指标 | 待复测 |
| 路由指标 | 待复测 |
| 生成指标 | 待复测 |
| 报告文件 | 待复测 |

### 5.1 离线发布准备证据（不构成正式模型指标）

| 日期 / 基线 | 命令 | 结果 | 说明 |
| --- | --- | --- | --- |
| 2026-08-11；base `24bf126` 上的未提交工作区 | `python scripts/eval_retrieval.py --fake` | 7 份文档、50 个向量/BM25 chunk、38 条案例 | 替身 Embedding 与 Rerank 仅验证数据切片、入库和指标流程；不得用于简历或正式效果结论。 |
| 2026-08-11；同上 | `python -m pytest tests -q` | 137 passed | Python 3.11 离线全量测试；不访问真实 LLM、Qdrant 网络或云服务。 |
| 2026-08-11；同上 | `npm run typecheck` / `npm run build` | 通过 | 类型检查与生产构建通过；不包含 Playwright。 |

发布阈值以根目录 `项目验收标准.md` 为准。

### 5.2 本地真实验收执行入口（待环境就绪后运行）

`backend/scripts/run_local_acceptance.py` 使用独立 Compose project、独立端口和数据卷，
依次执行真实 BGE Reranker 探测、知识库灌入、脱敏 HTTP 验收、现有 E2E/SSE 脚本、
真实检索/阈值评测与 Playwright。每次运行在 `backend/acceptance-evidence/<UTC 时间>/`
保存原始日志、JSON 摘要、浏览器报告和成功截图，不提交进 Git。

截至 2026-08-11，隔离项目 `copilot-acceptance-0811-evidence` 的 `WITH_RERANK=true` 镜像已启动；backend/frontend 均为 healthy，`/health` 与前端 HTTP 返回 200，认证后的 readiness 确认 CPU 上的 `BAAI/bge-reranker-base` 已启用。
容器内演示账号与 7 份语料初始化成功，得到 50 个 vector/BM25 chunk。脱敏 HTTP 验收 `acceptance.json` 的 11 项断言全部通过，覆盖 JWT 身份来源、用户隔离、注入/超长输入拦截、知识沉淀审核、写操作取消与批准一次性执行。
SSE 检查通过：总耗时 88.7s、首个 progress 事件 0.04s、最终收到 `done`。既有 E2E 检查通过并记录知识库重灌、只读工具、写操作确认、审计和安全拦截；两条 Playwright 注册登录流程均通过并保存报告、trace 与成功截图。
宿主机上的首次“真实”检索/阈值报告虽然标注了 `BGEReranker`，但 `rerank_applied=false` 时已静默回退到 RRF；其 0.03 左右的分数不能作为 BGE 指标或阈值结论。2026-08-11 对隔离容器的直接探针确认 BGE 已加载并对相关文本返回 0.982 归一化得分（`20-reranker-direct-probe.json`）。随后在同一容器内以真实 `EmbeddingClient`/`BGEReranker` 重跑 7 份语料、50 chunk、38 条案例：完整链路 Hit@5 为 86.8%、MRR 为 0.834，达到 85%/0.80 门槛；Rerank 实际应用于全部案例。阈值评测在 `min_rerank_score=0.12` 下 hit rate 为 78.9%、empty context 为 15.8%，通过“不全量清空”的运行门禁。证据为 `18-container-retrieval-report-20260811T083625Z.json`、`19-container-rerank-threshold-report-20260811T083625Z.json` 与两个 gate JSON。评测脚本现会在回退时以非零退出。
完整证据（含日志、脱敏 JSON、模型/语料哈希报告、Playwright 报告与截图）保留在被忽略的 `backend/acceptance-evidence/20260811T073000Z/`；不自动停止或清理该 Compose 现场。
所有前置、构建和失败现场证据保留在被忽略的 `backend/acceptance-evidence/`。
不得以离线 fake 结果替代该验收。脚本不运行 `eval_ragas.py`，Faithfulness 与 Answer
relevancy 继续为“未验证”。

### 2026-08-12：候选发布树的离线可复现性复核

当前未提交候选工作树使用 Python 3.11 重新执行 `pytest tests -q`，得到 139 passed、明确 exit 0；以 `uv export --frozen --extra dev --no-emit-project` 从 `uv.lock` 导出并安装到独立临时 Python 3.11 环境后，再次得到 139 passed、exit 0。前端 `npm.cmd ci`、`npm.cmd run typecheck` 与 `npm.cmd run build` 也均退出码 0。对应脱敏命令、版本、日志和退出码保存在 ignored evidence `20260812T-clean-clone/01-*`、`03-05-*`、`07-08-*`。

候选树已跟踪的 168 个文件未发现 `.env`、数据库、日志、coverage、模型缓存、构建产物或 acceptance evidence；高置信度密钥模式扫描同样无命中，输出不保存敏感值。初始 npm 安装报告 5 个依赖审计漏洞（1 moderate、4 high）。先将 `vite` 从 6.0.5 升至 6.4.3，再将 `react-router-dom` 从 6.28.0 升级至 7.18.2；Router v7 中已默认启用的 future 行为不再传入 `BrowserRouter`。升级后 `npm.cmd ci`、类型检查、生产构建、3 条无模型路由/权限 Playwright 回归均 exit 0，npm audit 为 0 vulnerabilities。既有两条真实模型 Playwright 流程也在复用的隔离 Docker 现场以 exit 0 完成（160.024s），因此本次依赖治理不再是发布前漏洞阻塞。

真正的 `HEAD 24bf126` 干净克隆则缺少候选工作树中尚未提交的 LICENSE、CI workflow、`uv.lock`、认证/初始化脚本、K8s 语料和前端登录资产，README 仍是旧版，因此不能作为候选版本的干净克隆通过证据。该结果是提交前阻塞，不应以候选工作树的本地等价结果替代；提交完成后必须在新的干净目录按 README 复测。远端 GitHub Actions、演示材料、Release 与付费/生成裁判均未执行。

### 2026-08-12：恢复、鲁棒性、安全与低并发证据

主机重启后，`copilot-acceptance-0811-evidence` 的 backend/frontend 自动恢复并再次 healthy；认证读取验证仍可取得 7 份知识库文档、11 个会话与 9 条工具审计（均为 HTTP 200）。这只证明本机隔离 Compose 卷可恢复，不外推为生产部署或干净克隆结论，证据为 `22-recovery-persistence.json`。

离线受控注入门禁 `30-resilience-report.json` 的 4 组、37 项测试均以退出码 0 通过，覆盖最大步数、工具失败、写操作拒绝/重复确认、Rerank 不可用时的 RRF 降级、LLM/Embedding 超时映射、SQLite 锁竞争及 trace 持久化。该层不实际断开 LLM、Embedding、Rerank 或数据库依赖，因此真实依赖故障演练仍未验证。

安全与可观测性报告 `31-security-observability-report.json` 验证生产模式拒绝弱 JWT/CORS、trace header 与持久化 trace 契约、命令证据不序列化运行时环境凭据；当前树 165 个文件及本轮 evidence 53 个文件的高置信度密钥模式扫描均无命中。该扫描不替代完整 Git 历史复核。

低并发真实服务报告 `32-low-concurrency-report.json` 在 CPU 本地模型环境执行 3 路 SSE 加 health/history 轮询：三路均收到 `done`，首个事件为 414–531ms，总耗时 307.9s；health 为 289/289、history 为 196/196，且未检测到 `database is locked`。这是本机隔离现场的性能基线，不作为生产容量承诺。
