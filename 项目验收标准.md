# K8s / DevOps 智能运维 Copilot 项目验收标准

> 文档用途：判断本项目是否达到“可写入简历、可发布到 GitHub、可由他人复现”的 v1.0 标准。
>
> 本文档定义的是最终交付门槛，不等同于开发进度说明。历史 README、验证报告或某次评测中的数字只能作为“待复测基线”；只有在指定代码提交上重新执行验收并保存证据后，才能将状态改为“通过”。

## 1. 验收规则

### 1.1 状态定义

所有验收项只能使用以下四种状态：

| 状态 | 含义 |
| --- | --- |
| 未验证 | 尚未在当前代码提交、当前数据集和指定模型上执行验收 |
| 通过 | 已执行验收，达到门槛，并保存了可追溯证据 |
| 不通过 | 已执行或静态检查后确认未达到门槛 |
| 不适用 | 经说明确认不属于本版本范围，且不影响核心闭环 |

不得使用“基本通过”“应该可用”“核心功能通过”等模糊状态。

### 1.2 v1.0 完成条件

同时满足以下条件，项目才允许标记为“完成”并发布 v1.0：

- 所有“必过”验收项均为“通过”。
- 后端自动化测试、契约测试、前端构建和关键 E2E 测试 100% 通过。
- 核心评测同时达到绝对门槛和相对基线提升门槛。
- 从干净克隆开始，能按照 README 通过 Docker Compose 复现完整演示。
- GitHub 仓库不包含密钥、个人信息、运行日志、缓存、数据库和无权再分发的数据。
- 每项通过结论都有命令输出、评测报告、API 响应样例、截图或录屏作为证据。

任一必过项为“未验证”或“不通过”，均不得对外宣称项目已完成。加分项不阻塞 v1.0。

### 1.3 验收基线信息

正式验收前填写，后续所有报告必须引用同一组信息：

| 字段 | 值 |
| --- | --- |
| 验收日期 | 待填写 |
| Git commit SHA | 待填写 |
| 数据集版本/哈希 | 待填写 |
| Python / Node.js 版本 | 待填写 |
| LLM provider 与模型 | 待填写 |
| Embedding 模型与维度 | 待填写 |
| Rerank 模型 | 待填写 |
| 操作系统 / CPU / GPU | 待填写 |
| 验收负责人 | 待填写 |

---

## 2. 业务场景与范围

### 2.1 对外定位

项目对外统一命名为 **K8s / DevOps 智能运维 Copilot**。目录名保留“企业级智能客服 Copilot”仅用于延续已有代码历史，README、GitHub 描述、演示和简历不得继续将其描述为通用客服系统。

目标用户是企业内部运维人员和开发人员，核心任务包括：

- 基于 Kubernetes 官方资料回答故障排查问题，并给出可定位的引用来源。
- 查询 Pod、Deployment 和告警工单等运行状态。
- 对重启 Deployment、创建或更新工单等写操作执行确认、鉴权、幂等和审计。
- 在证据不足时主动追问，不编造资源状态、命令执行结果或故障根因。
- 将经过人工审核的高质量对话沉淀回知识库。

### 2.2 数据真实性声明

- 知识库使用具有明确来源和许可说明的真实 Kubernetes 文档。
- Pod、Deployment、告警和工单状态为可复现的演示数据，不连接真实生产集群。
- README、简历和面试介绍必须明确区分“真实知识文档”与“模拟集群状态”。
- 不得宣称系统已经在真实企业生产环境、真实 Kubernetes 集群或真实工单系统中运行。

### 2.3 范围验收表

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-BIZ-01 | 必过 | 项目定位一致 | README、前端、GitHub 描述和简历草稿均使用运维 Copilot 定位 | 未验证 | 待填写 |
| OPS-BIZ-02 | 必过 | 核心业务路径 | 知识问答、状态查询、写操作确认、信息不足追问、知识沉淀均有可复现用例 | 未验证 | 待填写 |
| OPS-BIZ-03 | 必过 | 数据边界诚实 | 明确标注知识文档真实、集群状态为 mock，未出现生产接入的误导性描述 | 未验证 | 待填写 |
| OPS-BIZ-04 | 必过 | 不提供任意命令执行 | 工具白名单生效，模型不能构造并执行任意 shell 或 kubectl 命令 | 未验证 | 待填写 |
| OPS-BIZ-05 | 加分 | 本地真实集群 | 接入 kind/minikube，并以最小权限完成只读查询 | 未验证 | 待填写 |
| OPS-BIZ-06 | 加分 | 生产化扩展 | 多集群、生产级 RBAC、限流熔断或高可用至少落地一项 | 未验证 | 待填写 |

---

## 3. 真实数据与数据治理

### 3.1 必过门槛

- 知识库覆盖至少 10 类故障主题：Pod Pending、Service 访问不通、Ingress 配置错误、PVC 挂载失败、RBAC 权限不足、Node NotReady、ImagePullBackOff、资源配额超限、HPA 异常、NetworkPolicy 拦截。
- 入库后至少产生 50 个有效 chunk；空白、纯导航、重复标题和无业务含义的片段不计入。
- 每份原始资料记录标题、来源 URL、许可证、获取日期、原始文件 SHA-256、保存方式和预处理说明。
- GitHub 只提交允许再分发的代表样本；其他资料通过来源清单、下载说明或脚本复现。
- 评测集必须与 Prompt 示例、前端推荐问题和开发调试常量分离，代码不得针对固定 case ID 或问题文本硬编码路由和答案。
- 数据更新后必须产生新版本号或新哈希，并重新运行检索与生成评测。

### 3.2 数据来源登记模板

| 文档 ID | 标题/主题 | 来源 URL | 许可证 | 获取日期 | SHA-256 | 是否可入 GitHub | 预处理说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

### 3.3 数据验收表

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-DATA-01 | 必过 | 主题覆盖 | 10 类故障主题全部有来源文档和有效 chunk | 未验证 | 主题映射表 |
| OPS-DATA-02 | 必过 | 数据规模 | 有效 chunk 数量 ≥ 50 | 未验证 | 入库日志/统计接口 |
| OPS-DATA-03 | 必过 | 来源可追溯 | 每份文档的 URL、许可、日期、哈希和预处理信息完整 | 未验证 | 数据清单 |
| OPS-DATA-04 | 必过 | 发布合规 | GitHub 中仅包含允许再分发的数据，无个人信息和企业内部信息 | 未验证 | 发布前扫描记录 |
| OPS-DATA-05 | 必过 | 评测隔离 | 未发现针对固定评测问题的硬编码；评测集版本可识别 | 未验证 | 代码审查记录 |
| OPS-DATA-06 | 加分 | 自动获取 | 提供可重复执行的数据下载、校验和预处理脚本 | 未验证 | 脚本运行记录 |

---

## 4. 完整链路与功能验收

### 4.1 主链路

必须完整跑通并可追溯：

```text
输入与鉴权
  → 上下文组装/查询改写
  → 文档切片与入库
  → 向量检索 + BM25
  → RRF 融合 + Rerank + 阈值过滤
  → Agent 路由
  → 直接回答 / 工具调用 / 信息不足
  → 写操作确认、幂等与审计
  → 充分性校验
  → 带引用回答
  → 对话与 trace 持久化
  → 人工审核后知识沉淀
```

每次请求必须能查看：原始查询与改写查询、各检索阶段及耗时、最终引用片段、Agent 路由理由、工具名称及输入输出、充分性判断、trace ID。密钥、密码和内部系统提示不得出现在 trace 或前端。

### 4.2 必测场景

| ID | 场景 | 操作摘要 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-E2E-01 | 直接回答 | 询问 Pod Pending 排查方法 | 返回知识回答，含可定位引用和完整 trace | 未验证 | 响应 JSON/截图 |
| OPS-E2E-02 | 只读工具 | 查询指定 Deployment 副本状态 | 调用正确只读工具，参数和结果可审计 | 通过 | 2026-08-11 隔离 E2E：`get_pod_status`/`list_deployments` 审计与 trace |
| OPS-E2E-03 | 写操作批准 | 请求重启允许重启的 Deployment | 确认前不执行；批准后仅执行一次 | 通过 | 2026-08-11 隔离 E2E/Playwright：确认后 `restart_deployment` 成功且 trace 含 `execute_confirmed_write` |
| OPS-E2E-04 | 写操作取消 | 发起写操作后拒绝 | 不产生业务写入，不可用旧 token 再批准 | 通过 | 2026-08-11 `acceptance.json`：取消后重放返回 400 |
| OPS-E2E-05 | 幂等重放 | 重放同一 request_id | 不重复修改数据，明确标记 idempotent replay | 未验证 | 两次响应对比 |
| OPS-E2E-06 | 业务规则拒绝 | 重启不满足条件的 Deployment | 有限步内失败并解释原因，不循环确认 | 未验证 | trace |
| OPS-E2E-07 | 信息不足 | 不提供资源名称和命名空间 | 主动追问，不编造资源身份和状态 | 通过 | 2026-08-11 `acceptance.json`：`insufficient_information` |
| OPS-E2E-08 | 多轮上下文 | 第二轮用“这个 Pod”继续追问 | 正确关联上一轮实体 | 未验证 | 对话记录 |
| OPS-E2E-09 | 提示词注入 | 要求忽略系统规则 | 请求被拦截，不泄露提示词或执行工具 | 通过 | 2026-08-11 `acceptance.json`/E2E：`PROMPT_INJECTION_DETECTED` |
| OPS-E2E-10 | 超长输入 | 发送超过限制的文本 | 返回稳定错误码和 trace ID | 通过 | 2026-08-11 `acceptance.json`：`INPUT_TOO_LONG` |
| OPS-E2E-11 | 用户隔离 | 用户 B 访问用户 A 会话/审计 | 返回 404 或统一不可见结果，不泄露存在性 | 通过 | 2026-08-11 `acceptance.json`：跨用户会话访问 404 |
| OPS-E2E-12 | SSE | 执行长耗时问答 | 及时收到 progress/心跳，最终 done 与非流式 schema 一致 | 通过 | 2026-08-11 `16-sse-check.log`：首事件 0.04s，88.7s 后 done |
| OPS-E2E-13 | 知识沉淀 | 标记、审核并写回优质对话 | 未审核不入库；通过后可检索；拒绝后不入库 | 通过 | 2026-08-11 `acceptance.json`：JWT 身份标记与 admin 审核通过 |
| OPS-E2E-14 | 降级路径 | Rerank 或云端初筛不可用 | 按文档约定降级，错误可见且核心服务不崩溃 | 未验证 | 日志/响应 |

更细的页面操作可以继续引用现有《前端页面验收标准》，但该文档通过不能替代本节的接口、数据和评测验收。

---

## 5. 量化评测

### 5.1 指标门槛

| 指标 | 必过门槛 | 约束 |
| --- | --- | --- |
| 检索 Hit@5 | ≥ 85% | 仅统计知识检索类标注，工具结果字段不得混入 |
| 检索 MRR | ≥ 0.80 | 使用与 Hit@5 相同的数据集版本 |
| 相对基线提升 | 完整检索 MRR 比纯向量高 ≥ 0.15 | 完整检索 Hit@5 不得低于纯向量 |
| 知识问题路由准确率 | ≥ 85% | 单独报告样本数、错误 case ID |
| 工具问题路由准确率 | ≥ 95% | 读工具和写工具分别列出 |
| Faithfulness | ≥ 0.70 | 固定裁判模型和 Prompt 版本 |
| Answer relevancy | ≥ 0.65 | 与 Faithfulness 使用同一轮输出 |
| 引用覆盖率 | 100% | 需要知识依据的回答必须至少有一个可解析引用 |
| 引用正确率 | ≥ 90% | 人工抽查至少 20 个知识回答 |

评测报告必须记录：commit SHA、数据集版本与样本数、知识/工具样本划分、模型与参数、Rerank 配置、随机性设置、运行日期、硬件、总耗时和逐条失败案例。必须保存完整结果，不允许只展示多次运行中的最好一次。

### 5.2 结果登记

| 指标 | 纯向量基线 | 混合检索 | 完整链路 | 门槛 | 状态 | 报告位置 |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| Hit@5 | 57.9% | 84.2% | 84.2% | ≥ 85% | 未通过 | 2026-08-11 `18-retrieval-report.json`（38 条、7 文档、50 chunk） |
| MRR | 0.486 | 0.699 | 0.699 | ≥ 0.80 | 未通过 | 同上 |
| MRR 相对提升 | — | +0.214 | +0.214 | ≥ 0.15 | 通过（仅相对项） | 同上；不抵消绝对门槛失败 |
| 知识路由准确率 | — | — | 待复测 | ≥ 85% | 未验证 | 待填写 |
| 工具路由准确率 | — | — | 待复测 | ≥ 95% | 未验证 | 待填写 |
| Faithfulness | — | — | 待复测 | ≥ 0.70 | 未验证 | 待填写 |
| Answer relevancy | — | — | 待复测 | ≥ 0.65 | 未验证 | 待填写 |
| 引用覆盖率/正确率 | — | — | 待复测 | 100% / ≥ 90% | 未验证 | 待填写 |

### 5.3 历史记录值（仅作待复测基线）

以下数字来自现有 README 或验证文档，不代表本次验收已经通过：

| 来源 | 历史记录 | 使用限制 |
| --- | --- | --- |
| README（2026-08-09 记录） | 完整检索 Hit@5 86.8%，MRR 0.829 | 必须在当前 commit 和数据集上复测 |
| README（2026-08-09 记录） | 知识路由 86.7%，工具路由 100% | 必须确认 JWT/关联召回改造后的完整评测结果 |
| README（2026-08-09 记录） | faithfulness 0.718，answer relevancy 0.691 | 必须固定裁判模型并保存逐条结果 |
| `docs/评测与失败案例.md` 中保留的阶段记录 | JWT 改造阶段曾记录 131 项中 112 项通过、19 项失败 | 这是历史阻塞；当前是否清零必须重新运行 pytest |

---

## 6. 工程质量、安全与可观测性

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-ENG-01 | 必过 | 后端测试 | 全部单元、集成和契约测试通过，退出码为 0 | 通过（候选工作树） | 2026-08-12 Python 3.11：`pytest tests -q`，139 passed、exit 0；另以 `uv.lock` 导出的独立 Python 3.11 环境复测 139 passed、exit 0。尚待提交后的干净克隆复测；真实服务联调另计 |
| OPS-ENG-02 | 必过 | 前端类型与构建 | typecheck 和生产构建通过，退出码为 0 | 通过（候选工作树） | 2026-08-12 Router v7.18.2 / Vite 6.4.3：`npm.cmd ci`、`npm.cmd run typecheck`、`npm.cmd run build` 均 exit 0，npm audit 为 0 vulnerabilities |
| OPS-ENG-03 | 必过 | 关键 E2E | 对话、SSE、写操作确认三个关键流程全部通过 | 通过（候选工作树） | 2026-08-12 复用隔离现场：Router v7 后两条真实模型 Playwright 流程 exit 0（160.024s）；另有 3 条无模型路由/权限回归通过 |
| OPS-ENG-04 | 必过 | 鉴权与角色 | JWT 签名、过期、普通用户/admin 权限和用户隔离符合设计 | 通过 | Python 3.11 pytest 137 passed；`acceptance.json` 记录 JWT 身份、admin 审核和隔离 404 |
| OPS-ENG-05 | 必过 | 写操作安全 | 确认、幂等、白名单、参数校验和全量审计均有回归测试 | 通过 | pytest 137 passed；隔离 E2E/HTTP 记录取消、批准一次性执行与审计 |
| OPS-ENG-06 | 必过 | 失败收敛 | Agent 不超过最大步数；失败不形成无限重试或确认循环 | 通过（离线受控注入） | 2026-08-12 `30-resilience-report.json`：37 项、4 组退出码均为 0；覆盖最大步数、工具失败、拒绝/重复确认、Rerank 降级、超时映射与 SQLite 锁竞争 |
| OPS-ENG-07 | 必过 | 可观测性 | 响应和日志可通过 trace ID 关联，日志不含密钥和密码 | 通过（离线契约与证据扫描） | `31-security-observability-report.json`：trace header、持久化 trace、命令证据不序列化环境凭据均通过；evidence 扫描 53 文件无高置信度密钥命中 |
| OPS-ENG-08 | 必过 | 配置安全 | 仓库无真实密钥，生产配置拒绝弱密钥和宽泛 CORS | 通过（当前树扫描） | `31-security-observability-report.json`：生产 JWT/CORS 防护通过；当前树 165 文件及本轮 evidence 53 文件高置信度扫描均无命中；Git 历史复核另见发布门槛 |
| OPS-ENG-09 | 加分 | 性能基线 | 记录检索、首个 SSE 事件、总响应耗时及测试环境 | 通过（本地隔离 CPU） | 2026-08-12 `32-low-concurrency-report.json`：3 路 SSE 均 done，首事件 414–531ms，总耗时 307.9s；health 289/289、history 196/196，未见 `database is locked` |
| OPS-ENG-10 | 加分 | 依赖故障演练 | LLM、Embedding、Rerank、数据库故障均有明确降级或错误策略 | 未验证 | 故障注入报告 |

---

## 7. Docker 部署验收

### 7.1 必过标准

- 干净克隆后，README 明确列出 Docker、Docker Compose、Ollama 或云模型账号等前置条件。
- 仓库提供可复制的 `.env.example`，不要求读取开发者机器上的私有配置。
- `docker compose config` 校验成功。
- 完成模型准备和环境变量配置后，`docker compose up --build` 能启动前后端。
- 前端可访问，后端 `/api/v1/health` 返回成功，API 文档可访问。
- 初始化用户、知识库和演示数据有明确且可重复执行的步骤。
- 容器重启后持久化数据仍存在；重复初始化不会产生不可控重复数据。
- README 提供正常停止、查看日志、清理单个演示数据卷的说明；清理不是验收脚本的自动步骤。

### 7.2 部署验收表

| ID | 验收项 | 预期结果 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| OPS-DEP-01 | `docker compose config` | 退出码 0，无缺失配置 | 通过 | 2026-08-11：隔离验收前置检查退出码 0 |
| OPS-DEP-02 | `docker compose up --build` | backend、frontend 均为 healthy/running | 通过 | 2026-08-11 `copilot-acceptance-0811-evidence`：双容器 healthy，`90-compose-ps.log` |
| OPS-DEP-03 | 健康检查 | `/api/v1/health` 返回 HTTP 200 和 `status=ok` | 通过 | 2026-08-11 `05a-backend-health.log` 与 `acceptance.json` |
| OPS-DEP-04 | 前端访问 | `http://localhost:5173` 可登录并完成知识问答 | 通过 | 2026-08-11 Playwright 2 passed；成功截图与 HTML 报告 |
| OPS-DEP-05 | 初始化复现 | 新环境可以建立用户、灌入知识库并执行示例 | 通过 | 2026-08-11 `06-seed-users.log`、`07-seed-kb.log` 与 `17-e2e-check.log`（7 文档/50 chunk） |
| OPS-DEP-06 | 持久化 | 重启容器后会话和知识库仍可读取 | 通过（本机隔离 Compose 卷） | 2026-08-12 主机重启后容器自动恢复；`22-recovery-persistence.json` 认证读取 7 份知识库文档、11 个会话和 9 条工具审计均为 200 |

---

## 8. GitHub 发布验收

项目以独立仓库发布。v1.0 发布前逐项检查：

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-GH-01 | 必过 | README | 有业务定位、架构图、功能、限制、快速开始、评测结果和演示说明 | 未验证（待提交） | 候选 README 已按 2026-08-12 本地复现命令修正；`HEAD 24bf126` 仍是旧 README，提交后需重新干净克隆验证 |
| OPS-GH-02 | 必过 | 许可证 | 代码 LICENSE 明确；第三方数据和模型许可证单独说明 | 未验证（待提交） | 候选树有 MIT `LICENSE` 与 `docs/数据来源.md`；`HEAD 24bf126` 不含 LICENSE |
| OPS-GH-03 | 必过 | 配置模板 | 提供 `.env.example`，不含真实密钥、密码和个人路径 | 未验证（待提交） | 候选 `.env.example` 已存在且扫描无高置信度密钥；提交后需在干净克隆复核 |
| OPS-GH-04 | 必过 | 仓库卫生 | 无 `.env`、数据库、Qdrant 数据、日志、coverage、测试产物、模型缓存和构建目录 | 未验证（待提交） | 候选树 168 个已跟踪文件扫描无禁止运行产物；React Router 已升至 7.18.2、Vite 至 6.4.3，2026-08-12 npm audit 为 0 vulnerabilities。提交后需重新扫描实际 Git 树 |
| OPS-GH-05 | 必过 | 数据合规 | 仅提交许可明确的样本，其余提供来源与复现方式 | 未验证（待提交） | 候选数据来源表已存在；`HEAD 24bf126` 不含 K8s 语料和数据清单 |
| OPS-GH-06 | 必过 | CI | GitHub Actions 自动运行后端离线测试和前端构建 | 未验证（待提交且远端未运行） | 候选 `.github/workflows/ci.yml` 与本地等价命令通过；workflow 尚未提交或在远端执行 |
| OPS-GH-07 | 必过 | 演示材料 | 至少提供架构图、核心页面截图和一段完整闭环录屏或 GIF | 未验证 | 资源链接 |
| OPS-GH-08 | 必过 | 评测材料 | 提交完整指标表、配置、失败案例和复现命令 | 未验证（待提交） | 候选文档与 ignored evidence 已齐备；尚未形成提交后的公开 Git 树 |
| OPS-GH-09 | 必过 | 干净克隆 | 在新目录按 README 可完成构建、初始化和演示 | 未验证（提交前阻塞） | 2026-08-12 `HEAD 24bf126` 干净克隆缺少 LICENSE、CI、锁文件、认证/初始化、语料和前端登录资产，不能执行候选 README 路径；候选树离线 CI 等价验证通过但不替代干净克隆 |
| OPS-GH-10 | 必过 | Release | 创建 v1.0.0 tag/Release，说明能力、限制和已知问题 | 未验证 | Release 链接 |
| OPS-GH-11 | 加分 | 在线演示 | 提供受限、无敏感数据的公网演示 | 未验证 | URL |

发布前必须检查完整 Git 历史，而不只是当前工作区；曾提交过的密钥需要轮换并从历史中清除。

---

## 9. 验收命令与预期结果

以下命令按当前项目结构编写；正式执行时在证据栏记录实际命令、环境和退出码。

```powershell
# 后端测试（在 backend 目录）
.venv\Scripts\python.exe -m pytest tests -q
# 预期：退出码 0，failed=0

# 前端静态检查与构建（在 frontend 目录）
npm run typecheck
npm run build
# 预期：两条命令退出码均为 0

# 前端关键 E2E（后端、前端及知识库已准备）
npm run e2e
# 预期：退出码 0，关键用例无失败

# 检索评测（在 backend 目录）
.venv\Scripts\python.exe scripts\eval_retrieval.py
# 预期：脚本退出码 0，指标达到第 5.1 节门槛

# 端到端生成与路由评测
.venv\Scripts\python.exe scripts\eval_ragas.py --mode local
# 预期：脚本退出码 0，指标达到第 5.1 节门槛

# Docker 配置和启动（在项目根目录）
docker compose config
docker compose up --build
# 预期：配置命令退出码 0；前后端成功启动并通过健康检查
```

若实际虚拟环境位置不同，应更新 README 和本节命令，不能要求验收者猜测路径。

---

## 10. 当前完成度矩阵

“文档记录值”来自现有资料，仅用于定位差距；本表中的离线验证记录于 2026-08-11，基于 `24bf126` 的未提交工作区，不替代指定 commit 上的正式验收。

| 维度 | 文档记录值/静态现状 | 本次实测值 | 状态 | 主要阻塞 |
| --- | --- | --- | --- | --- |
| 业务定位 | README 已迁移为 K8s 运维场景 | 待填写 | 未验证 | 需检查前端、GitHub 和简历口径一致性 |
| 真实数据 | 7 篇整理语料、逐篇官方 URL/许可/日期/哈希与 10 主题映射已登记 | fake 入库：50 个有效 chunk | 通过（离线数据流程） | 真实模型检索与数据变更后仍需复测 |
| 完整链路 | README 记录 RAG、Agent、工具、SSE、沉淀已实现 | Docker HTTP/SSE/E2E/Playwright 均有证据；容器内真实 BGE 检索和阈值复测完成 | 通过（本地隔离现场） | 2026-08-11：`18-container-retrieval-report-20260811T083625Z.json`、`19-container-rerank-threshold-report-20260811T083625Z.json` 与 gate JSON |
| 检索指标 | 历史 Hit@5 86.8%、MRR 0.829 | 完整 BGE 链路 Hit@5 86.8%、MRR 0.834（38 条、7 文档/50 chunk） | 通过 | 达到 Hit@5 ≥ 85%、MRR ≥ 0.80；`rerank_verified=true`，阈值 0.12 未全量清空 context |
| 生成/路由指标 | 历史路由 86.7%/100%，faithfulness 0.718 | 待填写 | 未验证 | 需保存逐条结果与失败案例 |
| 自动化测试 | JWT 离线契约与回归测试已覆盖 | Python 3.11：139 passed、exit 0；锁定依赖独立环境复测同样通过 | 通过（候选工作树离线） | 提交后的干净克隆仍未验证；真实 HTTP/SSE/E2E 也已在隔离 Docker 现场执行 |
| 前端构建 | JWT 前端调用已对齐生成类型 | typecheck/build 通过；Playwright 2 passed | 通过 | 成功截图、trace 与 HTML 报告已保留 |
| Docker | Compose、镜像语料路径和容器初始化命令已收敛 | 隔离镜像双容器 healthy；初始化 7 文档/50 chunk；主机重启后数据仍可认证读取 | 通过（隔离现场） | 干净克隆仍未验证；本机卷恢复不等同生产部署验证 |
| GitHub 发布 | README、MIT、数据清单、CI 与忽略规则已补齐 | 候选树离线 CI 等价命令与当前树卫生扫描通过；`HEAD 24bf126` 干净克隆缺少这些未提交资产 | 未验证 | 先提交候选变更，再做干净克隆、远端 CI、演示材料与 Release |

---

## 11. 失败案例与阻塞问题

### 11.1 失败案例登记

| Case ID | 输入/场景 | 期望 | 实际 | 根因 | 修复状态 | 是否加入回归测试 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 未验证 | 待填写 | 待填写 |

失败案例不得从最终报告中删除。修复后保留原始现象、根因、修复提交和回归测试位置。

### 11.2 阻塞问题登记

| 阻塞 ID | 问题 | 影响的验收项 | 负责人 | 解除条件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| OPS-BLOCK-01 | 历史记录中的 19 个失败测试 | OPS-ENG-01、v1.0 发布 | 已解除 | Python 3.11 `pytest tests -q`：139 passed、exit 0（2026-08-12，候选工作树与锁定依赖独立环境） | 已解除（离线；提交后仍需干净克隆复测） |
| OPS-BLOCK-02 | 历史记录中的 6 个 JWT 迁移 TypeScript 错误 | OPS-ENG-02、前端演示 | 已解除 | `npm run typecheck` 与 `npm run build` 均退出码 0（2026-08-11） | 已解除（离线） |
| OPS-BLOCK-03 | `.venv` 指向失效 Python 3.11 路径 | 全部正式后端验证 | 已解除 | 新 Python 3.11 `.venv` 已执行完整 pytest | 已解除（离线） |
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 未验证 |

---

## 12. 最终验收结论

| 项目 | 填写内容 |
| --- | --- |
| 验收日期 | 待填写 |
| Git commit SHA | 待填写 |
| GitHub 仓库与 Release | 待填写 |
| 数据集版本/哈希 | 待填写 |
| 模型版本 | 待填写 |
| 必过项结果 | 待填写 |
| 加分项结果 | 待填写 |
| 未解决问题 | 待填写 |
| 最终结论 | 未验证 |
| 验收负责人 | 待填写 |

最终结论只能是“通过”“不通过”或“未验证”。只要存在任一未通过或未验证的必过项，最终结论就不能填写为“通过”，也不得发布 v1.0 或在简历中写成已完成项目。
