# K8s / DevOps 智能运维 Copilot 项目验收标准

> 文档用途：判断本项目是否达到“可写入简历、可发布到 GitHub、可由他人复现”的 v1.0 标准。
>
> 本文档定义的是最终交付门槛，不等同于开发进度说明。历史 README、验证报告或某次评测中的数字只能作为“待复测基线”；只有在指定代码提交上重新执行验收并保存证据后，才能将状态改为“通过”。

## 1. 验收规则

### 1.1 状态定义

所有验收项只能使用以下四种状态：

| 状态 | 含义 |
| --- | --- |
| 未验证 | 尚未在当前代码提交、当前数据集和指定模型上执行验收 |
| 通过 | 已执行验收，达到门槛，并保存了可追溯证据 |
| 不通过 | 已执行或静态检查后确认未达到门槛 |
| 不适用 | 经说明确认不属于本版本范围，且不影响核心闭环 |

不得使用“基本通过”“应该可用”“核心功能通过”等模糊状态。

### 1.2 v1.0 完成条件

同时满足以下条件，项目才允许标记为“完成”并发布 v1.0：

- 所有“必过”验收项均为“通过”。
- 后端自动化测试、契约测试、前端构建和关键 E2E 测试 100% 通过。
- 核心评测同时达到绝对门槛和相对基线提升门槛。
- 从干净克隆开始，能按照 README 通过 Docker Compose 复现完整演示。
- GitHub 仓库不包含密钥、个人信息、运行日志、缓存、数据库和无权再分发的数据。
- 每项通过结论都有命令输出、评测报告、API 响应样例、截图或录屏作为证据。

任一必过项为“未验证”或“不通过”，均不得对外宣称项目已完成。加分项不阻塞 v1.0。

### 1.3 验收基线信息

正式验收前填写，后续所有报告必须引用同一组信息：

| 字段 | 值 |
| --- | --- |
| 验收日期 | 2026-08-15（本地最终候选、Docker 完整链路与演示 GIF 已复验；远端发布仍在执行） |
| Git commit SHA | 签名实现候选 `a65080ab48f576d6e8cbbb120256ebb47cce09c5`；tree `33f85ce368119b06e3dc90d338e456373b92f4c5` |
| 数据集版本/哈希 | `2026-08-12-routing-diagnosis-v2`；固定 39 条；SHA-256 `f061f7db826a3aa28fac995bd9d28bb11db5c126e97db1ec9063a2679c11d79d` |
| Python / Node.js 版本 | Python 3.11.15 / Node.js 24.18.0；uv 0.12.2 / npm 11.16.0 |
| LLM provider 与模型 | DeepSeek V4 Pro 发布生成基准与 Docker 后端主链路；Qwen-Max 仅作独立参考裁判；离线克隆阶段 0 次模型调用，Docker 阶段未运行付费质量评测 |
| Embedding 模型与维度 | Ollama BGE-M3，1024 维 |
| Rerank 模型 | `BAAI/bge-reranker-base` |
| 操作系统 / CPU / GPU | 待填写 |
| 验收负责人 | 待填写 |

---

## 2. 业务场景与范围

### 2.1 对外定位

项目对外统一命名为 **K8s / DevOps 智能运维 Copilot**。目录名保留“企业级智能客服 Copilot”仅用于延续已有代码历史，README、GitHub 描述、演示和简历不得继续将其描述为通用客服系统。

目标用户是企业内部运维人员和开发人员，核心任务包括：

- 基于 Kubernetes 官方资料回答故障排查问题，并给出可定位的引用来源。
- 查询 Pod、Deployment 和告警工单等运行状态。
- 对重启 Deployment、创建或更新工单等写操作执行确认、鉴权、幂等和审计。
- 在证据不足时主动追问，不编造资源状态、命令执行结果或故障根因。
- 将经过人工审核的高质量对话沉淀回知识库。

### 2.2 数据真实性声明

- 知识库使用具有明确来源和许可说明的真实 Kubernetes 文档。
- Pod、Deployment、告警和工单状态为可复现的演示数据，不连接真实生产集群。
- README、简历和面试介绍必须明确区分“真实知识文档”与“模拟集群状态”。
- 不得宣称系统已经在真实企业生产环境、真实 Kubernetes 集群或真实工单系统中运行。

### 2.3 范围验收表

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-BIZ-01 | 必过 | 项目定位一致 | README、前端、GitHub 描述和简历草稿均使用运维 Copilot 定位 | 未验证 | 待填写 |
| OPS-BIZ-02 | 必过 | 核心业务路径 | 知识问答、状态查询、写操作确认、信息不足追问、知识沉淀均有可复现用例 | 通过 | commit `a65080ab`：Compose `15191` DeepSeek 知识问答 `1/1`；Docker 主流程覆盖只读状态查询、写操作确认与信息不足；`20260814T140355Z` 覆盖知识沉淀拒绝不入库和批准后可检索 |
| OPS-BIZ-03 | 必过 | 数据边界诚实 | 明确标注知识文档真实、集群状态为 mock，未出现生产接入的误导性描述 | 未验证 | 待填写 |
| OPS-BIZ-04 | 必过 | 不提供任意命令执行 | 工具白名单生效，模型不能构造并执行任意 shell 或 kubectl 命令 | 未验证 | 待填写 |
| OPS-BIZ-05 | 加分 | 本地真实集群 | 接入 kind/minikube，并以最小权限完成只读查询 | 未验证 | 待填写 |
| OPS-BIZ-06 | 加分 | 生产化扩展 | 多集群、生产级 RBAC、限流熔断或高可用至少落地一项 | 未验证 | 待填写 |

---

## 3. 真实数据与数据治理

### 3.1 必过门槛

- 知识库覆盖至少 10 类故障主题：Pod Pending、Service 访问不通、Ingress 配置错误、PVC 挂载失败、RBAC 权限不足、Node NotReady、ImagePullBackOff、资源配额超限、HPA 异常、NetworkPolicy 拦截。
- 入库后至少产生 50 个有效 chunk；空白、纯导航、重复标题和无业务含义的片段不计入。
- 每份原始资料记录标题、来源 URL、许可证、获取日期、原始文件 SHA-256、保存方式和预处理说明。
- GitHub 只提交允许再分发的代表样本；其他资料通过来源清单、下载说明或脚本复现。
- 评测集必须与 Prompt 示例、前端推荐问题和开发调试常量分离，代码不得针对固定 case ID 或问题文本硬编码路由和答案。
- 数据更新后必须产生新版本号或新哈希，并重新运行检索与生成评测。

### 3.2 数据来源登记模板

| 文档 ID | 标题/主题 | 来源 URL | 许可证 | 获取日期 | SHA-256 | 是否可入 GitHub | 预处理说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

### 3.3 数据验收表

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-DATA-01 | 必过 | 主题覆盖 | 10 类故障主题全部有来源文档和有效 chunk | 未验证 | 主题映射表 |
| OPS-DATA-02 | 必过 | 数据规模 | 有效 chunk 数量 ≥ 50 | 未验证 | 入库日志/统计接口 |
| OPS-DATA-03 | 必过 | 来源可追溯 | 每份文档的 URL、许可、日期、哈希和预处理信息完整 | 未验证 | 数据清单 |
| OPS-DATA-04 | 必过 | 发布合规 | GitHub 中仅包含允许再分发的数据，无个人信息和企业内部信息 | 未验证 | 发布前扫描记录 |
| OPS-DATA-05 | 必过 | 评测隔离 | 未发现针对固定评测问题的硬编码；评测集版本可识别 | 未验证 | 代码审查记录 |
| OPS-DATA-06 | 加分 | 自动获取 | 提供可重复执行的数据下载、校验和预处理脚本 | 未验证 | 脚本运行记录 |

---

## 4. 完整链路与功能验收

### 4.1 主链路

必须完整跑通并可追溯：

```text
输入与鉴权
  → 上下文组装/查询改写
  → 文档切片与入库
  → 向量检索 + BM25
  → RRF 融合 + Rerank + 阈值过滤
  → Agent 路由
  → 直接回答 / 工具调用 / 信息不足
  → 写操作确认、幂等与审计
  → 充分性校验
  → 带引用回答
  → 对话与 trace 持久化
  → 人工审核后知识沉淀
```

每次请求必须能查看：原始查询与改写查询、各检索阶段及耗时、最终引用片段、Agent 路由理由、工具名称及输入输出、充分性判断、trace ID。密钥、密码和内部系统提示不得出现在 trace 或前端。

### 4.2 必测场景

| ID | 场景 | 操作摘要 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-E2E-01 | 直接回答 | 询问 Pod Pending 排查方法 | 返回知识回答，含可定位引用和完整 trace | 通过 | `20260814T114238Z-final-candidate-docker-deepseek-readiness-fixed/acceptance.json`：knowledge answer HTTP 200、9 个引用、完整检索与生成步骤 |
| OPS-E2E-02 | 只读工具 | 查询指定 Deployment 副本状态 | 调用正确只读工具，参数和结果可审计 | 通过 | 同一 `acceptance.json`：`get_pod_status` 成功、只读审计存在、无 cache/replay |
| OPS-E2E-03 | 写操作批准 | 请求重启允许重启的 Deployment | 确认前不执行；批准后仅执行一次 | 通过 | 同一 `acceptance.json` 与 E2E：`restart_deployment` 批准后单次执行，trace 含 `execute_confirmed_write` 与重复调用跳过 |
| OPS-E2E-04 | 写操作取消 | 发起写操作后拒绝 | 不产生业务写入，不可用旧 token 再批准 | 通过 | 同一 `acceptance.json`：取消结果为 `write_rejected`，旧确认 token 重放返回 400 |
| OPS-E2E-05 | 幂等重放 | 重放同一 request_id | 不重复修改数据，明确标记 idempotent replay | 未验证 | 两次响应对比 |
| OPS-E2E-06 | 业务规则拒绝 | 重启不满足条件的 Deployment | 有限步内失败并解释原因，不循环确认 | 未验证 | trace |
| OPS-E2E-07 | 信息不足 | 不提供资源名称和命名空间 | 主动追问，不编造资源身份和状态 | 通过 | 同一 `acceptance.json`：`insufficient_information` 安全收敛，无工具调用 |
| OPS-E2E-08 | 多轮上下文 | 第二轮用“这个 Pod”继续追问 | 正确关联上一轮实体 | 未验证 | 对话记录 |
| OPS-E2E-09 | 提示词注入 | 要求忽略系统规则 | 请求被拦截，不泄露提示词或执行工具 | 通过 | 同一 `acceptance.json`：HTTP 422、`PROMPT_INJECTION_DETECTED` |
| OPS-E2E-10 | 超长输入 | 发送超过限制的文本 | 返回稳定错误码和 trace ID | 通过 | 同一 `acceptance.json`：HTTP 422、`INPUT_TOO_LONG` |
| OPS-E2E-11 | 用户隔离 | 用户 B 访问用户 A 会话/审计 | 返回 404 或统一不可见结果，不泄露存在性 | 通过 | 同一 `acceptance.json`：跨用户会话访问 404 |
| OPS-E2E-12 | SSE | 执行长耗时问答 | 及时收到 progress/心跳，最终 done 与非流式 schema 一致 | 通过 | `sse-check.log`：流程 exit 0，最终事件为 `done`；SHA-256 `add07e5a2d435b3ce9ad7c0721e67b9b185932d2a4f85b9a5d9a7546901f3099` |
| OPS-E2E-13 | 知识沉淀 | 标记、审核并写回优质对话 | 未审核不入库；通过后可检索；拒绝后不入库 | 通过 | `20260814T140355Z-final-candidate-docker-frontend-sedimentation`：真实 admin 浏览器审核；拒绝项 `kb_document_id=null` 且知识库/vector/BM25 canonical SHA 前后相同；批准文档新增 3 chunks 并由生产 Retriever 最终过滤命中 |
| OPS-E2E-14 | 降级路径 | Rerank 或云端初筛不可用 | 按文档约定降级，错误可见且核心服务不崩溃 | 未验证 | 日志/响应 |

更细的页面操作可以继续引用现有《前端页面验收标准》，但该文档通过不能替代本节的接口、数据和评测验收。

---

## 5. 量化评测

### 5.1 指标门槛

| 指标 | 必过门槛 | 约束 |
| --- | --- | --- |
| 检索 Hit@5 | ≥ 85% | 仅统计知识检索类标注，工具结果字段不得混入 |
| 检索 MRR | ≥ 0.80 | 使用与 Hit@5 相同的数据集版本 |
| hard 子集 MRR | ≥ 0.90 | 固定 17 条 hard case，ID 与顺序不得漂移 |
| hard 子集 Hit@3 | = 100% | 固定 17 条 hard case，不能用全量指标替代 |
| 检索非回归 | 完整链路 Hit@5 不低于纯向量，hard MRR 不低于纯向量 | 全量与 hard 的相对变化继续报告为诊断项 |
| Rerank 增益 | 完整链路 hard MRR 严格高于 hybrid | 相等也不通过，必须证明 Rerank 有正增益 |
| Rerank 有效性 | `rerank_verified=true` | fallback 或任一未执行 Rerank 的 case 均不能作为通过 |
| 知识问题路由准确率 | ≥ 85% | 单独报告样本数、错误 case ID |
| 工具问题路由准确率 | ≥ 95% | 读工具和写工具分别列出 |
| Faithfulness | ≥ 0.70 | 固定裁判模型和 Prompt 版本 |
| Answer relevancy | ≥ 0.65 | 与 Faithfulness 使用同一轮输出 |
| 引用覆盖率 | 100% | 需要知识依据的回答必须至少有一个可解析引用 |
| 引用正确率 | ≥ 90% | 人工抽查至少 20 个知识回答 |

评测报告必须记录：commit SHA、数据集版本与样本数、知识/工具样本划分、模型与参数、Rerank 配置、随机性设置、运行日期、硬件、总耗时和逐条失败案例。必须保存完整结果，不允许只展示多次运行中的最好一次。

### 5.2 结果登记

| 指标 | 纯向量基线 | 混合检索 | 完整链路 | 门槛 | 状态 | 报告位置 |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| Hit@5 | 100% | 100% | 100% | ≥ 85% | 通过 | `20260814T144019Z-hard-mrr-gate-contract-bge/20-retrieval-report.json`：30 条知识 case、7 文档/51 chunk |
| MRR | 0.919444 | 0.927778 | 0.944444 | ≥ 0.80 | 通过 | 同上；完整链路实际使用 `BGEReranker` |
| hard 子集 MRR | 0.887255 | 0.901961 | 0.931373 | ≥ 0.90 | 通过 | 固定 17 条 hard case，ID/顺序和容器输入 SHA-256 未漂移 |
| hard 子集 Hit@3 | 94.1% | 100% | 100% | = 100% | 通过 | 完整链路达到 17/17 |
| Hit@5 非回归 | 100% | 100% | 100% | 完整链路 ≥ 纯向量 | 通过 | `1.0 >= 1.0` |
| hard MRR 非回归 | 0.887255 | 0.901961 | 0.931373 | 完整链路 ≥ 纯向量 | 通过 | `0.931373 >= 0.887255` |
| Rerank hard MRR 增益 | — | 0.901961 | 0.931373 | 完整链路 > hybrid | 通过 | 增益 `+0.029412` |
| Rerank 有效性 | — | — | `rerank_verified=true` | 必须为 true | 通过 | 30/30 已评测、无 fallback 或失败原因 |
| hard MRR 相对纯向量提升（历史诊断） | — | +0.014706 | +0.044118 | 如实报告 | 不适用 | 旧阻塞阈值 `+0.15` 要求 hard MRR `1.037255`，超过理论上限 `1.0`；原值保留但不再作为 gate |
| 全量 MRR 相对提升（诊断项） | — | +0.008333 | +0.025000 | 如实报告 | 不适用 | 完整链路相对纯向量为 +0.025000，仅作诊断项 |
| 知识路由准确率 | — | — | 100% | ≥ 85% | 通过 | 2026-08-14 DeepSeek V4 Pro + BGE-M3 完整 39 条严格门禁 |
| 工具路由准确率 | — | — | 只读 100%；写确认 100% | ≥ 95% | 通过 | 同上；q36 成功工具结果进入最终 `T1` 证据映射 |
| Faithfulness | — | — | Qwen-Max v2 正式复裁 `0.9821` | ≥ 0.70 | 通过 | 39/39 调用成功且参考聚合超过门槛；原始单项理由保留，Qwen 不作为发布唯一裁判，q35 经生产 Schema 独立确认的提示缺口已确定性修复 |
| Answer relevancy | — | — | Qwen-Max v2 正式复裁 `0.9103` | ≥ 0.65 | 通过 | 参考聚合超过门槛；q24/q35b 为裁判聚焦度或复合 UI 分歧，Qwen 仅作参考，不构成单独 hard gate |
| 引用覆盖率/正确率 | — | — | 覆盖率 100%；固定 q01-q20 及工具/写操作单人复核通过 | 100% / ≥ 90% | 通过 | `acceptance-evidence/20260814T060050Z-deepseek-39-route-final/`；回答、原子、chunk、工具 Pointer 与标签逐项一致 |

本阶段最新诊断探针（不覆盖上表历史 38 条全量结论）：q03、q19、q35、q35b、q36 在隔离 backend 容器中运行 `--no-judge`，结果 `exit_code=2`、严格通过 `3/5`。q35/q35b/q36 通过；q03/q19 的正确候选在 `min_rerank_score=0.12` 过滤后均未进入最终上下文，分别表现为误调只读工具和 `max_steps_exceeded`。证据：`backend/acceptance-evidence/20260812T181132Z-routing-diagnosis-probes/probes.json`、`strict-summary.json`；本轮 Python 3.11 全量回归 `150 passed`、exit 0 仅作为离线回归证据。严格门禁未全通过，引用人工审阅、Faithfulness、Answer relevancy 和 Qwen 裁判保持未验证。

q03/q19 分层专项诊断已进一步确认：两题在 vector、BM25、RRF、Rerank 各层均保留 3 个相关候选，Rerank Top-3 未发生正确内容掉出 Top-N；但 q03 最高相关归一化分数为 `0.0528`、q19 为 `0.0330`，均被 `min_rerank_score=0.12` 过滤，阈值后相关候选均为 0。此项仅证明当前 BGE 分数与阈值校准不匹配，不构成降低阈值的授权或生成/路由指标通过。证据：`backend/acceptance-evidence/20260812T184250Z-q03-q19-layer-diagnosis/layer-diagnosis.json`；同一候选工作树的 Python 3.11 全量回归为 `153 passed`、`exit_code=0`。完整严格门禁、引用人工审阅、Faithfulness、Answer relevancy 继续未验证。
固定 39 条阈值敏感性复测已增加评测集顺序/数量/哈希护栏，针对性测试 `3 passed`。新的隔离现场 `backend/acceptance-evidence/20260812T113603Z-rerank-threshold-39/` 成功确认 39 条案例、7 份文档和 50 个 chunk，但 BGE Reranker 全部降级为 `RERANKER_UNAVAILABLE`，`scored_case_count=0`、`rerank_verified=false`、阈值结果为空并以 exit 1 停止。因此本轮无有效敏感性指标、无最小阈值建议，不能据此调整阈值；需先恢复可用 Reranker 后重跑。
随后补齐已锁定的 `FlagEmbedding==1.3.3` rerank extra，在 `backend/acceptance-evidence/20260812T115205Z-rerank-threshold-39-bge/rerank-threshold-report.json` 完成真实 BGE 固定 39 条扫描，`scored_case_count=39`、`rerank_verified=true`。当前 `0.12` 为命中 `76.9%`、空上下文 `15.4%`、平均噪声 `1.41`；候选 `0.05` 为命中 `79.5%`、空上下文 `10.3%`、平均噪声 `1.49`，但仍无法保留 q19 的 `0.0330` 相关候选；`0.00` 虽命中 `89.7%`，平均噪声升至 `10.15`。因此扫描仅形成校准证据，不构成自动降低阈值的授权；最小修复需另立子阶段并建立 RED 回归。

### 2026-08-12：知识问答专用阈值修复验收

实现 `knowledge_min_rerank_score=0.03`，仅供无实时资源目标、无写操作意图的知识问答；实时查询与写操作继续使用全局 `min_rerank_score=0.12`。全局生产阈值、Prompt、语料、评测集、Docker 和 Git 均未改动。

Python 3.11 定向测试 `43 passed`，全量后端回归 `163 passed`。真实 BGE 39 条阈值报告 `backend/acceptance-evidence/-knowledge-threshold-39/rerank-threshold-report.json` 确认 39/39、7 文档/50 chunk、实际 `BGEReranker`，`0.03` 的总体命中 82.1%、hard 命中 100.0%、空上下文 7.7%、平均噪声 1.56；该指标不替代全局阈值门槛。目录名因 PowerShell `Get-Date -AsUTC` 不兼容缺少 UTC 前缀，原始隔离现场完整保留。

真实本地 Ollama/BGE `--no-judge` 报告 `backend/acceptance-evidence/20260812T123741Z-knowledge-threshold-no-judge/no-judge-results.json` 以 exit 0 完成 39/39 严格门禁：知识路由、只读工具、写确认和引用覆盖均 100%，q03/q19 均为 direct_answer 且保留引用；未调用 Qwen，Faithfulness/Answer relevancy 为 not_run。故本子阶段“知识问答专用阈值策略”通过，但“全局检索阈值修复”“Qwen 生成质量”“远端 CI”“Release”仍未验证，不得将项目最终验收标为通过。

固定 q01-q20 的工程复核已完成：来源为上述 39/39 无付费报告（SHA-256 `6827ace9d4f4494207ffa9a7511803c99bba7e795cf8e2fa1532f415de5d8e88`），20/20 引用可按原始 Markdown 文档、heading path 与 chunk ID 定位；但仅 17/20（85.0%）核心结论有无误引的直接支持，低于引用正确率 `>= 90%` 门槛，状态为不通过。失败为 q03（混入 ImagePullBackOff 等待时间来源）、q12（Waiting 状态断言无来源）和 q19（权限绑定立即生效断言无来源）。这是单一工程复核，不是独立双人审阅，也不替代 Qwen Faithfulness/Answer relevancy。证据：ignored `backend/acceptance-evidence/20260812T211729Z-citation-review-release-preflight/citation-review-summary.json`、`citation-review.md`。

同目录本地只读发布前静态核查确认 `.gitignore` 覆盖环境、数据库、Qdrant、evidence、测试/构建产物，追踪路径未发现对应运行产物；许可证、数据许可、评测隔离与 mock/未验证边界都有本地文件依据。当前脏工作树存在 ignored 未追踪 `backend_pytest.log`，后续提交不得加入。本核查不等同完整 Git 历史内容级密钥扫描、远端 CI、演示录屏或 Release 验收。

### 2026-08-12：q03/q12 最小证据约束收口

q03/q12 的 Answerer 证据约束测试已通过：`2 passed, 1 xfailed`；知识阈值策略、Chat 契约和 no-judge 相关回归为 `40 passed`；Python 3.11 后端全量回归为 `166 passed, 1 xfailed`、exit 0。该结果只验证本地 owner 约束与离线回归，不替代真实模型引用正确率复测。

q19 未通过且保持阻塞。官方 Kubernetes RBAC 资料没有直接证明“权限绑定变更即时生效且无需重启”，所以未修改本地语料、gold answer、评测规则、Prompt 或任何阈值；现有 `kubectl auth can-i` 仅为验证命令。20 条工程复核的引用正确率仍为 17/20（85.0%），低于 `>= 90%` 门槛，项目最终验收、Faithfulness、Answer relevancy、远端 CI、演示材料和 Release 继续为未验证。证据：ignored `backend/acceptance-evidence/20260812T135417Z-q03-q12-closeout-q19-evidence-block/`。

### 2026-08-12：q19 标准答案重新界定验收

经授权，q19 不再验收“即时生效/无需重启”；当前标准答案只验收官方直接支持的 Kubernetes API 动态配置、RoleBinding/ClusterRoleBinding 授权和 `kubectl auth can-i` 实际权限验证。新语料 SHA-256 为 `865BB566DEF39C576203246D6BDE6BDB7D9D9EF5A53046F45DC29B17B7E2123C`，真实 BGE 现场为 7 文档/51 chunk。

新现场 `20260812T140200Z-q19-redefined-rerank-threshold-39/` 确认 39/39、BGEReranker 实际启用且无降级；`20260812T144000Z-q19-redefined-no-judge/no-judge-results.json` exit 0、39/39 严格通过，未调用 Qwen。固定 q01-q20 工程复核为 18/20（90.0%），20/20 可定位，q19 已通过；q03、q12 各保留一个生成误引问题，因此该项仅为达到引用门槛，不是三题全部修复，也不等同最终发布通过。最终 Python 3.11 回归为 169 passed。证据汇总：`backend/acceptance-evidence/20260812T144000Z-q19-redefined-citation-review/`。

### 5.3 历史记录值（仅作待复测基线）

以下数字来自现有 README 或验证文档，不代表本次验收已经通过：

| 来源 | 历史记录 | 使用限制 |
| --- | --- | --- |
| README（2026-08-09 记录） | 完整检索 Hit@5 86.8%，MRR 0.829 | 必须在当前 commit 和数据集上复测 |
| README（2026-08-09 记录） | 知识路由 86.7%，工具路由 100% | 必须确认 JWT/关联召回改造后的完整评测结果 |
| README（2026-08-09 记录） | faithfulness 0.718，answer relevancy 0.691 | 必须固定裁判模型并保存逐条结果 |
| `docs/评测与失败案例.md` 中保留的阶段记录 | JWT 改造阶段曾记录 131 项中 112 项通过、19 项失败 | 这是历史阻塞；当前是否清零必须重新运行 pytest |

---

## 6. 工程质量、安全与可观测性

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-ENG-01 | 必过 | 后端测试 | 全部单元、集成和契约测试通过，退出码为 0 | 通过 | commit `a65080ab` 的独立克隆：Python 3.11.15 锁定安装后全量 `298 passed`、契约 `70 passed`，均 exit 0 |
| OPS-ENG-02 | 必过 | 前端类型与构建 | typecheck 和生产构建通过，退出码为 0 | 通过 | commit `a65080ab` 在 Node 24.18.0 / npm 11.16.0 下执行 npm ci、类型生成无漂移、typecheck、build 和 audit，均 exit 0，0 vulnerabilities；远端 CI 的 Node 20 实跑另计 |
| OPS-ENG-03 | 必过 | 关键 E2E | 对话、SSE、写操作确认三个关键流程全部通过 | 通过 | commit `a65080ab` 的 DeepSeek Docker backend 完成 HTTP/JWT 12/12、E2E、SSE `done`及写操作拒绝/批准/单次执行 |
| OPS-ENG-04 | 必过 | 鉴权与角色 | JWT 签名、过期、普通用户/admin 权限和用户隔离符合设计 | 通过 | commit `a65080ab` 的 Python 3.11 全量 `298 passed`、契约 `70 passed`；当前 Docker HTTP 证据记录 JWT 身份、admin 审核和隔离 404 |
| OPS-ENG-05 | 必过 | 写操作安全 | 确认、幂等、白名单、参数校验和全量审计均有回归测试 | 通过 | commit `a65080ab` 的离线 `298/70` 契约与当前 Docker E2E/HTTP 共同记录取消、批准单次执行及审计 |
| OPS-ENG-06 | 必过 | 失败收敛 | Agent 不超过最大步数；失败不形成无限重试或确认循环 | 通过 | 2026-08-12 `30-resilience-report.json`：37 项、4 组退出码均为 0；覆盖最大步数、工具失败、拒绝/重复确认、Rerank 降级、超时映射与 SQLite 锁竞争 |
| OPS-ENG-07 | 必过 | 可观测性 | 响应和日志可通过 trace ID 关联，日志不含密钥和密码 | 通过 | commit `a65080ab` 的安全与可观测性回归 `14 passed`；最新 Compose 前端问答补证中 source 173 文件、evidence 23 个可解析文本文件及 trace 压缩包专项扫描均 0 findings，27 文件 SHA-256 清单复算一致 |
| OPS-ENG-08 | 必过 | 配置安全 | 仓库无真实密钥，生产配置拒绝弱密钥和宽泛 CORS | 通过 | commit `a65080ab` 源树扫描 0 findings；Docker 收口后克隆临时 `.env` 已单文件删除，source/clone tracked 状态干净；Git 历史复核另见发布门槛 |
| OPS-ENG-09 | 加分 | 性能基线 | 记录检索、首个 SSE 事件、总响应耗时及测试环境 | 通过 | 2026-08-12 `32-low-concurrency-report.json`：3 路 SSE 均 done，首事件 414–531ms，总耗时 307.9s；health 289/289、history 196/196，未见 `database is locked` |
| OPS-ENG-10 | 加分 | 依赖故障演练 | LLM、Embedding、Rerank、数据库故障均有明确降级或错误策略 | 未验证 | 故障注入报告 |

---

## 7. Docker 部署验收

### 7.1 必过标准

- 干净克隆后，README 明确列出 Docker、Docker Compose、Ollama 或云模型账号等前置条件。
- 仓库提供可复制的 `.env.example`，不要求读取开发者机器上的私有配置。
- `docker compose config` 校验成功。
- 完成模型准备和环境变量配置后，`docker compose up --build` 能启动前后端。
- 前端可访问，后端 `/api/v1/health` 返回成功，API 文档可访问。
- 初始化用户、知识库和演示数据有明确且可重复执行的步骤。
- 容器重启后持久化数据仍存在；重复初始化不会产生不可控重复数据。
- README 提供正常停止、查看日志、清理单个演示数据卷的说明；清理不是验收脚本的自动步骤。

### 7.2 部署验收表

| ID | 验收项 | 预期结果 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| OPS-DEP-01 | `docker compose config` | 退出码 0，无缺失配置 | 通过 | commit `a65080ab` 独立克隆的最小 DeepSeek 配置经生产 `Settings` 和 `docker compose config --quiet` 预检通过 |
| OPS-DEP-02 | `docker compose up --build` | backend、frontend 均为 healthy/running | 通过 | `copilot-final-deepseek-ready-114238`：backend image `sha256:edbb25c0dd02b8b9a0086967d03087f0fb0a35752875a87da2d446f3e56a01b3`、frontend `sha256:0228dd3b9d548ad43176fc8d0c095e78ca0d12aedb2cc24bfc52373ad2737b44`，双容器 healthy |
| OPS-DEP-03 | 健康检查 | `/api/v1/health` 返回 HTTP 200 和 `status=ok` | 通过 | `20260814T114238Z-final-candidate-docker-deepseek-readiness-fixed/10-backend-health.json`、认证 readiness 七项通过 |
| OPS-DEP-04 | 前端访问 | Compose frontend `http://localhost:15191` 可登录并完成知识问答 | 通过 | `20260814T151334Z-final-candidate-compose-frontend-knowledge-qa/`：直连 `15191` 的 DeepSeek 知识问答 Playwright `1/1`、retries 0；一次 `/chat/stream`，verified 生成、真实 hybrid/Rerank、原始语料引用与历史 trace 持久化均通过 |
| OPS-DEP-05 | 初始化复现 | 新环境可以建立用户、灌入知识库并执行示例 | 通过 | 用户与知识库初始化通过，重复初始化保持 7 文档/51 chunk/51 vector/51 BM25；HTTP/JWT 12/12 与 E2E 通过 |
| OPS-DEP-06 | 持久化 | 重启容器后会话和知识库仍可读取 | 通过 | 重启前后 canonical SHA-256 均为 `761b76bb83958fe17989993c4d55b6111b7c9078a7a3f5b79e833ddec8351932`，保持 9 conversations/28 messages/8 audits、7/51/51/51 和 7 项 API 复查一致 |

---

## 8. GitHub 发布验收

项目以独立仓库发布。v1.0 发布前逐项检查：

| ID | 级别 | 验收项 | 通过标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| OPS-GH-01 | 必过 | README | 有业务定位、架构图、功能、限制、快速开始、评测结果和演示说明 | 通过 | commit `a65080ab` 的 README 包含所需内容；独立干净克隆完成离线构建、Docker 初始化、API 核心链路和持久化复验，未验证项已明确列出 |
| OPS-GH-02 | 必过 | 许可证 | 代码 LICENSE 明确；第三方数据和模型许可证单独说明 | 通过 | commit `a65080ab` 包含 MIT `LICENSE` 与 `docs/数据来源.md`；7 份 K8s 整理语料逐篇登记官方 URL、CC BY 4.0 说明、日期和 SHA-256 |
| OPS-GH-03 | 必过 | 配置模板 | 提供 `.env.example`，不含真实密钥、密码和个人路径 | 通过 | commit `a65080ab` 包含 `backend/.env.example`；私有配置未进索引，源及 Docker evidence 扫描 0 findings |
| OPS-GH-04 | 必过 | 仓库卫生 | 无 `.env`、数据库、Qdrant 数据、日志、coverage、测试产物、模型缓存和构建目录 | 通过 | commit `a65080ab` 最终 HEAD/tree 和 tracked status 无漂移；运行产物均 ignored，临时 `.env` 已删除，npm audit 为 0 vulnerabilities |
| OPS-GH-05 | 必过 | 数据合规 | 仅提交许可明确的样本，其余提供来源与复现方式 | 通过 | commit `a65080ab` 包含 7 份 K8s 语料及来源/许可/哈希清单；未包含运行数据或模型缓存 |
| OPS-GH-06 | 必过 | CI | GitHub Actions 自动运行后端离线测试和前端构建 | 通过 | [run 31821164169](https://github.com/qianlin-code/k8s-devops-copilot/actions/runs/31821164169)：`c65b271` 的 Python 3.11 后端完整测试和 Node 20 前端 typecheck/build 均绿色；无 workflow 修复 |
| OPS-GH-07 | 必过 | 演示材料 | 至少提供架构图、核心页面截图和一段完整闭环录屏或 GIF | 通过 | README 架构图及 `docs/assets/demo.gif`；本地 Ollama/Compose 实跑，1280x720、约 27 秒、177255 bytes，覆盖登录、知识问答、引用、Trace 与历史记录，画面脱敏检查通过 |
| OPS-GH-08 | 必过 | 评测材料 | 提交完整指标表、配置、失败案例和复现命令 | 通过 | commit `a65080ab` 包含评测文档、数据清单、评测配置和复现命令；运行证据按约定保留在 ignored evidence |
| OPS-GH-09 | 必过 | 干净克隆 | 在新目录按 README 可完成构建、初始化和演示 | 通过 | commit `a65080ab` 独立克隆完成依赖安装、298/70、生成物、安全扫描、前端构建/audit、隔离 DeepSeek Docker 初始化、API 主链路、持久化及 Compose frontend `15191` 路由/权限、知识审核与真实知识问答演示 |
| OPS-GH-10 | 必过 | Release | 创建 v1.0.0 tag/Release，说明能力、限制和已知问题 | 未验证（执行中） | [预期 v1.0.0 Release](https://github.com/qianlin-code/k8s-devops-copilot/releases/tag/v1.0.0)；`RELEASE_NOTES.md` 与 GIF 资产已就绪，待签名 tag、tag CI 和 Release 实际创建 |
| OPS-GH-11 | 加分 | 在线演示 | 提供受限、无敏感数据的公网演示 | 未验证 | URL |

发布前必须检查完整 Git 历史，而不只是当前工作区；曾提交过的密钥需要轮换并从历史中清除。

---

## 9. 验收命令与预期结果

以下命令按当前项目结构编写；正式执行时在证据栏记录实际命令、环境和退出码。

```powershell
# 后端测试（在 backend 目录）
.venv\Scripts\python.exe -m pytest tests -q
# 预期：退出码 0，failed=0

# 前端静态检查与构建（在 frontend 目录）
npm run typecheck
npm run build
# 预期：两条命令退出码均为 0

# 前端关键 E2E（后端、前端及知识库已准备）
npm run e2e
# 预期：退出码 0，关键用例无失败

# 检索评测（在 backend 目录）
.venv\Scripts\python.exe scripts\eval_retrieval.py
# 预期：脚本退出码 0，指标达到第 5.1 节门槛

# 端到端生成与路由评测
.venv\Scripts\python.exe scripts\eval_ragas.py --mode local
# 预期：脚本退出码 0，指标达到第 5.1 节门槛

# Docker 配置和启动（在项目根目录）
docker compose config
docker compose up --build
# 预期：配置命令退出码 0；前后端成功启动并通过健康检查
```

若实际虚拟环境位置不同，应更新 README 和本节命令，不能要求验收者猜测路径。

---

## 10. 当前完成度矩阵

“文档记录值”来自现有资料，仅用于定位差距；本表汇总截至 2026-08-14 的当前证据。离线与 Docker 候选证据均绑定签名实现 commit `a65080ab`；历史 `31d6551`/`376e38f` 证据只保留过程记录，不能覆盖当前检索合同复验或远端发布门禁。

| 维度 | 文档记录值/静态现状 | 本次实测值 | 状态 | 主要阻塞 |
| --- | --- | --- | --- | --- |
| 业务定位 | README 已迁移为 K8s 运维场景 | 待填写 | 未验证 | 需检查前端、GitHub 和简历口径一致性 |
| 真实数据 | 7 篇整理语料、逐篇官方 URL/许可/日期/哈希与 10 主题映射已登记 | 当前候选隔离容器初始化保持 7 文档/51 chunk/51 vector/51 BM25 | 通过 | 数据或分块逻辑变化后需复测 |
| 完整链路 | README 记录 RAG、Agent、工具、SSE、沉淀已实现 | commit `a65080ab` 的 DeepSeek Docker 完成 HTTP/JWT 12/12、E2E、SSE `done`、写确认/审计与重启持久化；Compose frontend `15191` 路由/权限 `3/3`、知识审核 `1/1`、真实知识问答 `1/1`，拒绝不入库、批准后可检索和历史 trace 持久化通过 | 通过 | 本地完整链路已闭合；远端 CI 与生产发布门禁另计 |
| 检索指标 | 历史 Hit@5 86.8%、MRR 0.829 | 当前完整 BGE 链路 Hit@5 100%、MRR 0.944444、hard MRR 0.931373、hard Hit@3 100%；相对 hybrid hard MRR +0.029412，Rerank verified | 通过 | 八项修正合同全部通过；旧 `+0.15` 要求 1.037255，保留为数学不可达的历史诊断 |
| 生成/路由指标 | 历史路由 86.7%/100%，faithfulness 0.718 | DeepSeek V4 Pro + BGE-M3 固定 39 条严格评测 39/39；q35 确定性提示离线门禁通过；Qwen-Max v2 为 0.9821/0.9103 | 通过 | 生成质量证据绑定现有固定报告；Docker 阈值 39/39 不冒充新的生成评测 |
| 自动化测试 | JWT 离线契约与回归测试已覆盖 | commit `a65080ab` 的干净克隆以 Python 3.11.15 锁定依赖执行全量 298 passed、契约 70 passed | 通过 | 远端 CI 仍未执行 |
| 前端构建 | JWT 前端调用已对齐生成类型 | commit `a65080ab` 干净克隆的类型生成无漂移，npm ci/typecheck/build/audit 全部通过，0 vulnerabilities | 通过 | Node 20 的 GitHub Actions 实跑仍未执行 |
| Docker | Compose、镜像语料路径和容器初始化命令已收敛 | commit `a65080ab` 独立克隆的隔离 Compose 双容器健康，初始化、HTTP/JWT、SSE、E2E、重启持久化、`15191` 浏览器交互、知识沉淀完整语义与 DeepSeek 知识问答通过 | 通过 | 本地 Docker 验收已闭合；不等同生产部署或远端发布 |
| GitHub 发布 | README、MIT、数据清单、CI 与忽略规则已补齐 | 公开仓库、`main`、MIT、脱敏 GIF 和首轮远端 CI 已通过；发布准备 commit `c65b271` 使用 GitHub noreply 邮箱并经 SSH 签名 | 未验证（Release 执行中） | 仍需文档提交 CI、签名 tag、tag CI 与 Release |

2026-08-13 候选补充结论：Python 3.11 `187 passed`，前端 typecheck/build 通过；39 条真实无付费机器门禁 `39/39`。但 q01-q20 人工逐句复核发现 q02/q04/q05/q07/q16/q17/q18 仍有无来源命令、写操作建议或扩展结论，因此生成/引用质量仍为不通过，Qwen 裁判、Docker 最终候选复验、远端 CI 和 Release 均未执行。

2026-08-14 Qwen 复裁时的候选结论：写参数来源、单主题诊断、根因/步骤完整性、工具确定性摘要和裁判上下文均已按通用协议收口。DeepSeek V4 Pro + BGE-M3 + BGE Reranker 的最新完整报告为 `39/39`，全部生成 verified、无 fallback，路由和引用指标 100%。Qwen-Max v2 正式复裁 39/39 次调用成功，Faithfulness `0.9821`、Answer relevancy `0.9103`；裁判费用 `0.904960 元`，生成与裁判合计 `1.018691 元`。q24/q35b 复核为裁判分歧，q35 当时没有提示 description 至少 10 个字符，属于实质缺陷。

2026-08-15 当前候选补充结论：q35 已从生产 Tool Schema 通用渲染字符串长度约束，readiness 探针也已在不放宽业务截断规则的前提下修复。签名实现候选 `a65080ab` 在独立克隆通过 Python 3.11 全量 `298 passed`、契约 `70 passed`、生成物、安全、前端构建与 audit；DeepSeek Docker 后端主链路、Compose 双容器健康和重启持久化通过。后续无付费模型补证直接在 Compose frontend `15191` 通过路由/权限和知识审核交互，并验证拒绝不入库、批准后可检索。hard-MRR 旧差值合同因要求 `1.037255 > 1.0` 被确认数学上不可达；替换为绝对质量、非回归和真实 Rerank 八项合同后，固定输入 Docker 复验全部通过。最后以唯一一次 DeepSeek V4 Pro 顶层问题完成 Compose frontend `15191` 知识问答与历史持久化，知识索引保持不变。本地 Ollama/Compose 演示 GIF 已生成并完成画面脱敏检查。本地完整链路、Docker 与演示材料验收通过；远端 CI、tag 和 Release 仍未验证，DeepSeek V4 Flash 未接入，项目尚未发布。

---

## 11. 失败案例与阻塞问题

### 11.1 失败案例登记

| Case ID | 输入/场景 | 期望 | 实际 | 根因 | 修复状态 | 是否加入回归测试 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GEN-ROUTE-20260812 | 38 条本地 Ollama/BGE 严格生成、路由与引用评测 | 30 条知识直接回答、6 条只读工具精确调用、2 条写确认且不执行写入 | 13/38 严格通过；25 条失败，包含 `max_steps_exceeded`、`insufficient_information`、误调工具及 q35 写确认缺失 | 待独立诊断：本地模型结构化路由与充分性流程在真实链路中未稳定收敛 | 未修复 | 已有 `test_eval_ragas_no_judge.py` 锁定门禁判定；故障修复后需新增 owner 回归 | `20260812T150000Z-no-judge-evaluation/02-no-judge-real.json`、`03-strict-gate-summary.json` |
| QWEN-V2-Q35-20260814 | 用户已给工单标题，但缺 namespace 与 description | 一次性说明缺失字段，并告知 description 至少需要 10 个字符；不提前确认或执行 | 原报告正确追问两个字段且未执行写入，但只显示字段名，没有最小长度约束 | 通用不足提示只渲染 Pydantic violation 的 field，丢失了生产 Tool Schema 的可操作约束 | 已修复（当前工作树） | 是；`test_contract_chat.py` 覆盖缺失、过短、未由用户提供及 title 长度约束 | 原始 `20260814T063012Z-qwen-max-formal-v2/`；当前 Python 3.11 契约 `70 passed`、全量 `290 passed` |

失败案例不得从最终报告中删除。修复后保留原始现象、根因、修复提交和回归测试位置。

### 11.2 阻塞问题登记

| 阻塞 ID | 问题 | 影响的验收项 | 负责人 | 解除条件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| OPS-BLOCK-01 | 历史记录中的 19 个失败测试 | OPS-ENG-01、v1.0 发布 | 已解除 | Python 3.11 `pytest tests -q`：commit `31d6551` 的干净克隆中 139 passed、exit 0（2026-08-12） | 已解除（离线；真实服务与远端 CI 另计） |
| OPS-BLOCK-02 | 历史记录中的 6 个 JWT 迁移 TypeScript 错误 | OPS-ENG-02、前端演示 | 已解除 | `npm run typecheck` 与 `npm run build` 均退出码 0（2026-08-11） | 已解除（离线） |
| OPS-BLOCK-03 | `.venv` 指向失效 Python 3.11 路径 | 全部正式后端验证 | 已解除 | 新 Python 3.11 `.venv` 已执行完整 pytest | 已解除（离线） |
| OPS-BLOCK-04 | Verified Answer 已阻止无来源扩写，但真实本地 7B 定向证据选择失败 | 生成/引用质量、Qwen 裁判、v1.0 发布 | Answerer/评测 owner | 新通用方案的真实定向集通过，再完成固定 39 条和 q01-q20 逐项复核 | 已解除（DeepSeek/BGE-M3 39/39，q01-q20 20/20；本地 7B 仍仅为离线兼容模式） |
| OPS-BLOCK-05 | 工具辅助回答可忽略成功工具结果，只选择知识 K 原子 | 实时工具正确性、生成/引用质量、Qwen 裁判、v1.0 发布 | Answerer/状态机 owner | 通用协议保证成功工具结果进入最终 T 映射；离线 RED→GREEN 后重跑真实 q36、固定 13 条、完整 39 条和 q01-q20 复核 | 已解除（q36 1/1、固定 13 条 13/13、完整 39 条 39/39，q01-q20 20/20） |
| OPS-BLOCK-06 | 旧裁判发现跨主题知识选择、写参数补造、工具摘要缺失及裁判上下文不足 | 生成相关性、工具安全、参考裁判 | Retriever/Answerer/工具/评测 owner | 通用协议修复后重新完成 DeepSeek 39 条、人工证据复核和 Qwen v2 正式复裁 | 已解除（新报告 39/39；Qwen v2 为 0.9821/0.9103；新 q35 提示缺口另列 OPS-BLOCK-07） |
| OPS-BLOCK-07 | 新 Qwen-Max v2 低分复核确认 q35 只列出缺失字段名，未提示 description 至少 10 个字符 | 写操作可操作性、生成相关性、v1.0 发布 | Agent 写参数不足提示 owner | 从生产 Tool Schema 通用渲染约束提示，完成 RED→GREEN；既有 DeepSeek q35 证明真实路由，新离线契约验证确定性展示；不得增加 q35 特例 | 已解除（定向 4、契约 70、全量 290 passed） |
| OPS-BLOCK-08 | 旧 hard-MRR 差值门槛要求从 BGE-M3 纯向量 `0.887255` 提升 `>=0.15`，目标 `1.037255` 超过 MRR 理论上限 | 检索量化门禁、v1.0 发布 | Retriever/Rerank 评测 owner | 不改模型、语料或 case；以 hard 绝对质量、非回归、Rerank 正增益和无 fallback 合同完成固定输入复验 | 已解除（新合同八项全通过；旧 `0.044117647 < 0.15` 保留为不可达历史诊断，不宣称达到旧阈值） |

---

## 12. 最终验收结论

| 项目 | 填写内容 |
| --- | --- |
| 验收日期 | 2026-08-15（最终候选离线、Docker 完整链路、hard-MRR 合同、前端知识问答与演示 GIF 已复验；远端发布仍未验证） |
| Git commit SHA | 签名实现候选 `a65080ab48f576d6e8cbbb120256ebb47cce09c5`；tree `33f85ce368119b06e3dc90d338e456373b92f4c5` |
| GitHub 仓库与 Release | [qianlin-code/k8s-devops-copilot](https://github.com/qianlin-code/k8s-devops-copilot)；[预期 v1.0.0 Release](https://github.com/qianlin-code/k8s-devops-copilot/releases/tag/v1.0.0) |
| 数据集版本/哈希 | `2026-08-12-routing-diagnosis-v2`；固定 39 条；SHA-256 `f061f7db826a3aa28fac995bd9d28bb11db5c126e97db1ec9063a2679c11d79d` |
| 模型版本 | 发布候选：DeepSeek V4 Pro + BGE-M3 + BGE Reranker；Ollama 7B 为离线兼容；Qwen-Max 为独立参考裁判 |
| 必过项结果 | 离线干净克隆、Docker 完整链路、`15191` 浏览器交互与真实知识问答、知识沉淀语义、修正后的 hard-MRR 门禁、脱敏 GIF、公开仓库和首轮远端 CI 通过；文档提交 CI、tag CI 与 Release 待完成 |
| 加分项结果 | 待填写 |
| 未解决问题 | 文档提交 CI、签名 tag、tag CI 与 Release 待完成 |
| 最终结论 | 未验证 |
| 验收负责人 | 待填写 |

最终结论只能是“通过”“不通过”或“未验证”。只要存在任一未通过或未验证的必过项，最终结论就不能填写为“通过”，也不得发布 v1.0 或在简历中写成已完成项目。
### 2026-08-12：q03/q12 Answerer 证据一致性收口（代码 GREEN，真实复测阻塞）

Answerer owner 已增加通用证据一致性后处理并通过 T2 定向测试：`7 passed`；相关契约测试合计 `30 passed`，系统 Python 3.13 全量 `172 passed` 仅作 fallback。后处理仅作用于知识片段且无工具结果的回答，q03 不再混入 ImagePullBackOff 的 `300 秒/5 分钟` 时长，q12 不再否定 `Waiting` 并区分 Pod 调度与容器启动阶段；工具、写操作和空上下文路径未改变。

项目要求的 Python 3.11 虚拟环境启动失败（uv trampoline `permission denied (os error 5)`），因此新的真实 BGE 39 条评测、无付费 `--no-judge` 和 q01-q20 引用复核均未运行。本轮不得登记 q03/q12 真实生成质量通过；q19 新口径及两个 Rerank 阈值未改变。证据：`backend/acceptance-evidence/20260812T-q03-q12-answerer-evidence-closeout/answerer-constraint-closeout.json`。

### 2026-08-13：Python 3.11 环境恢复复核仍未通过

新隔离环境 `backend/.venv-recheck311` 已创建并安装 Python 3.11.15、pytest、FlagEmbedding 和 torch；但解释器执行仍被 uv trampoline 的 `permission denied (os error 5)` 阻断，`uv run` 同样无法读取用户 uv Python 目录。受控执行申请因本地审批代理额度不足未获批准。本轮没有新的 Python 3.11、BGE、无付费严格门禁或 q01-q20 引用复核证据，q03/q12 真实生成质量继续为未验证。证据：`backend/acceptance-evidence/20260812T-q03-q12-answerer-evidence-closeout/python311-environment-recheck.json`。

### 2026-08-13：Verified Answer 候选验收状态

候选代码已实现结构化 K#/T# 证据选择、服务端精确验证、确定性渲染、最多两次尝试、信息不足降级、统一状态机出口、输出清洗复核、trace Schema 与前端映射展示。Python 3.11 全量回归 `200 passed, 16 skipped`，前端 typecheck/build 均 exit 0；16 个 skip 是已被新 verified-plan 套件取代的旧自由文本后处理测试。后端请求/响应、SSE 与数据库结构未变，新增 trace 字段有默认值并兼容历史 payload。

真实模型门禁未通过。最终 q02/q03 定向现场 `acceptance-evidence/20260813T-verified-answer-sections-q02-q03/` 为 `0/2`、exit 2，两条均在两次尝试后以 `no_evidence_selected` 安全降级为 `insufficient_information`。因此 OPS-E2E-01、知识路由/生成、引用覆盖与引用正确率不能登记为当前候选通过；旧候选的 39/39 只保留为历史结果。完整 39 条、q01-q20 人工审核、Qwen 费用预检和裁判均未运行，最终结论保持“未验证”，不得创建 v1.0.0 tag 或 Release。

### 2026-08-14：当前候选生成与引用验收状态

上述 2026-08-12/13 条目作为历史失败证据保留，不代表当前候选。当前候选的工具证据协议已完成 RED→GREEN；Python 3.11 全量 `268 passed`、契约 `63 passed`，前端 typecheck/build、密钥扫描与 `git diff --check` 通过。显式使用 DeepSeek V4 Pro + BGE-M3 + BGE Reranker 的 q36、固定 13 条和完整 39 条分别为 `1/1`、`13/13`、`39/39`，完整报告 exit 0，知识/只读工具/写确认路由与引用机器指标均为 100%。固定 q01-q20 单人证据复核为 `20/20`，摘要见 ignored `acceptance-evidence/20260814T025617Z-deepseek-bge-m3-39/manual-review-summary.json`。

qwen-max 正式裁判已完成，39 次调用全部成功，Faithfulness 与 Answer relevancy 均为 `0.8154`，裁判费用 `0.985380 元`，连同生成费用合计 `1.037948 元`。低分复核将 q31/q32/q33/q34/q35b/q36 归为缺少工具 T 原子或 pending_write 上下文的评测协议分歧，同时确认 q17/q22/q29/q35/q37 存在实质相关性或安全字段缺口。聚合门槛通过不能覆盖这些失败；默认模型切换、最终提交干净克隆、Docker 候选复验、远端 CI、演示材料、tag 与 Release 继续阻塞，最终结论保持“未验证/未发布”。

### 2026-08-14：DeepSeek 发布基准与确定性工具回答复验

上一段是历史正式裁判现场，原始分数和失败分类保留。当前工作树已经用非 case 特例的服务端规则修复跨主题选择、根因缺步骤、写参数补造、工具摘要和裁判上下文。最终离线门禁为 Python 3.11 全量 `289 passed`、独立契约 `69 passed`；前端 typecheck/build、密钥与安全扫描、`git diff --check` 均 exit 0。

最新 DeepSeek V4 Pro + BGE-M3 + BGE Reranker 报告 `acceptance-evidence/20260814T060050Z-deepseek-39-route-final/deepseek-39-report.json` 为 `39/39`、exit 0、无 fallback；知识/只读工具/写确认路由、引用覆盖和关键词支持均为 100%。q01-q20 与全部工具/写操作人工复核确认展示文本逐项对应知识原文或工具 Pointer；q29 仅含 NetworkPolicy，q35 同时追问 namespace/description 且没有确认/调用，q37 使用工具摘要明确禁止零可用副本重启。

新候选 Qwen-Max 零预算预检 `acceptance-evidence/20260814T061322Z-qwen-max-preflight/qwen-max-preflight.json` 为 exit 0、`judge_enabled=false`；预计裁判 `2.6364 元`，加 DeepSeek 生成共 `2.750131 元`。该预检随后获得单独费用授权并进入下述正式复裁。Docker、Git 提交、GitHub、远端 CI、录屏、tag 与 Release 在该阶段均未执行。

### 2026-08-14：Qwen-Max v2 正式参考复裁

对固定 DeepSeek 39 条报告只运行一次 qwen-max `ragas-judge-v2-verified-evidence`。输入报告 SHA-256 为 `bb5096eef0391b4a35702d66df73f29a6c10c666f81fa0da694ea4b94c0f0215`；39/39 次调用成功，Faithfulness `0.982051`、Answer relevancy `0.910256`，均超过聚合门槛。实际裁判费用 `0.904960 元`，连同生成费用共 `1.018691 元`，低于授权的 `20 元`总上限。正式报告 SHA-256 为 `28a9c4dc823d82e1572106019f2070a26ec98090892daab7bcffb5dd3b73af86`，证据位于 ignored `acceptance-evidence/20260814T063012Z-qwen-max-formal-v2/`。

3 条单项低分全部人工复核：q24 已直接给出 gold 所需的 `kubectl auth can-i`，仅因扩展排查内容被降相关性；q35b 的完整参数位于前端实际展示的 `pending_write` 确认卡，裁判上下文也包含该卡，两者登记为非阻塞裁判分歧。q35 正确追问 namespace/description 且没有执行写操作，但只显示字段名，没有说明 description 至少需要 10 个字符，属于新的实质可操作性缺陷。OPS-BLOCK-07 保持阻塞，最终结论继续为“未验证/未发布”；本阶段没有修改实现代码，也没有执行 Docker、Git/GitHub、远端 CI、录屏、tag 或 Release。

### 2026-08-14：q35 生产 Schema 约束提示离线收口

q35 后续在 Agent 写参数不足提示 owner 完成通用 RED→GREEN。缺失、非法或用户未明确提供的字段先按原始路径去重，再从对应 Tool 的 Pydantic JSON Schema 读取 `minLength/maxLength`，确定性显示 `description（至少 10 个字符）`、`title（4–120 个字符）`；未知字段退回原名，`request_id` 不展示。确认令牌、工具调用、写审计和参数来源安全规则保持不变。

Python 3.11 定向 `4 passed`、契约 `70 passed`、全量 `290 passed`。既有 DeepSeek 报告已证明 q35 会路由到 create_incident 并同时追问 namespace/description，本次变更仅修复服务端确定性展示，因此没有再次调用 DeepSeek、Qwen-Max 或其他付费模型。OPS-BLOCK-07 已解除。

发布生成质量以 DeepSeek V4 Pro 固定 39 条严格门禁、服务端证据映射和人工复核为主；Qwen-Max v2 的分数、理由和分歧证据作为非权威参考保留，只有获得生产 Schema、契约或人工证据独立支持的问题才形成阻塞，也不使用 DeepSeek 自评作为唯一裁判。默认模型组合未变，DeepSeek V4 Flash 未接入。截至该子阶段，项目仍等待最终 SHA 与干净克隆；后续签名候选结果见下一节，最终结论始终保持“未验证”。

### 2026-08-14：签名实现候选与离线干净克隆验收

生成器修复后，原 75 路径加 `backend/scripts/export_api_examples.py` 与 `backend/tests/test_export_api_examples.py` 形成 77 路径实现冻结提交 `947007d7453adb36ba38ca9c6d65626dbb9708c5`，tree `deae77e86f663028d97bd4a9471fd1ed68883351`。该提交通过 SSH 签名验证，但独立克隆的最终 npm audit 命中 `nanoid 3.3.17` 的高危公告 `GHSA-2v37-7h3g-55p8`，因此候选按门禁失败并保留现场。授权的一次 lockfile-only 安装随后没有改变任何文件、仍解析到 3.3.17，未被误记为修复成功。

最终通过 `overrides.nanoid=3.3.18` 与 lockfile 共同锁定传递依赖，形成签名补丁提交 `376e38f5e2490f29a78127c3fc82aa863d5c2c10`、tree `753c7c4c69f01e27ab1123712612ba5a9b13e6f9`。补丁相对 `947007d` 只修改 `frontend/package.json` 与 `frontend/package-lock.json`；相对 `31d6551` 的累计候选边界为 `67 modified + 12 added = 79`，无删除。两个提交均由 Ed25519 指纹 `SHA256:pw6HtxZ3a2ldJsvwSISWcmq3t4CDgkhxh2Dp1MCVBkI` 验证通过。

commit `376e38f` 的 `%LOCALAPPDATA%\Temp\k8s-copilot-clean-clone-20260814T080915Z-376e38f5` 独立克隆使用 Python 3.11.15 / uv 0.12.2 完成 frozen dev export 与 50 包安装；前端类型生成无漂移，25 个 API 语义快照通过；后端全量 `296 passed in 104.39s`、契约 `70 passed in 27.61s`；安全与可观测性通过，整仓密钥扫描 215/215、0 findings。Node 24.18.0 / npm 11.16.0 下 npm ci、`nanoid 3.3.18` 唯一解析、typecheck、build 和 audit 0 vulnerabilities 全部通过。最终 HEAD/tree、tracked status 和 `git diff --check` 无漂移，克隆中没有 `.env`、evidence、数据库或日志，仅保留预期 ignored 依赖、构建和测试缓存。

生成器 RED/GREEN 阶段还保留一项外部调用事故：修复前 3 次导出意外到达 Qwen 沉淀质量筛选，未打印密钥，具体费用无法确定；修复后的生成器门禁和最终干净克隆均为零 Provider 调用。事故、失败 audit、无效 lockfile-only 尝试和最终通过汇总分别保存在 ignored `acceptance-evidence/20260814T072015Z-git-boundary-clean-clone-v2/`、`acceptance-evidence/20260814T080245Z-nanoid-3.3.18-clean-clone/` 与 `acceptance-evidence/20260814T080534Z-nanoid-override-clean-clone/`。

截至该历史子阶段，离线候选门禁通过不等于项目最终验收通过；当时没有运行 Docker、付费模型、V4 Flash、push、远端 CI、录屏、tag 或 Release，OPS-GH-09 仍为未验证。后续 readiness 修复和当前候选 Docker 结果以下一节为准，整体结论始终保持“未验证/未发布”。

### 2026-08-14：Readiness 修复与当前候选 DeepSeek Docker 验收

旧候选 Docker 失败现场全部保留：首次 Ollama 冷启动超时；Ollama 结果命中 hard-MRR 门禁失败；DeepSeek 临时 CORS JSON 多写反斜杠导致 Settings 解析失败；修正配置后的 readiness 探针又因 `max_tokens=5` 返回截断；首次持久化快照还因 PowerShell 重写 `python -c` 引号输出空文件。这些失败没有被覆盖或删除，也没有被表述为模型生成质量失败。

Readiness 根因通过最小实现修复：探针明确要求只回复 `OK`，使用 `temperature=0`、`max_tokens=32`；`LLMClient` 对 `finish_reason=length`、空响应和其他协议错误的拒绝仍保持。新增参数/指令回归与截断异常透传回归通过，连同既有业务截断测试证明没有豁免真实截断。签名实现候选 `a65080ab48f576d6e8cbbb120256ebb47cce09c5`、tree `33f85ce368119b06e3dc90d338e456373b92f4c5` 通过指纹 `SHA256:pw6HtxZ3a2ldJsvwSISWcmq3t4CDgkhxh2Dp1MCVBkI` 验签；独立克隆 `%LOCALAPPDATA%\Temp\k8s-copilot-readiness-20260814T112009Z-a65080ab` 完成 Python 3.11 全量 `298 passed`、契约 `70 passed`、前端 typecheck/build/audit、生成物、安全和仓库卫生复验。

唯一 DeepSeek V4 Pro 顶层 Docker 主流程在项目 `copilot-final-deepseek-ready-114238` 通过双容器健康、认证 readiness、7 文档/51 chunk/51 vector/51 BM25、HTTP/JWT `12/12`、E2E、SSE `done`、知识引用、只读审计、写操作拒绝/批准/单次执行、真实 `BGEReranker` 无 fallback 与阈值案例 `39/39`。宿主 Vite `5173` 代理 Docker backend 的 Playwright 为 `5/5`，Compose frontend `15191` 只验证健康和 HTTP 200，未做浏览器交互。HTTP acceptance、SSE、阈值报告和 Playwright 日志 SHA-256 分别为 `3fbab44c27e3ce9e5d6686b407c6d144f88096ce2a7d60b329071eb5395b61d4`、`add07e5a2d435b3ce9ad7c0721e67b9b185932d2a4f85b9a5d9a7546901f3099`、`1b07a4bce0fa62b088ad00315d6dfb5455b7111ec3396411d2ca7209a9e77a2a`、`109af9a27cda91ea93fa5728f0c2261027f53ca24c406a37340e7c9d221a0ec3`。这里的阈值 `39/39` 不是新的付费生成质量评测；生成质量继续引用既有 DeepSeek 固定 `39/39` 与人工证据。知识沉淀只验证了标记、审核通过和生成文档 ID，批准后可检索及拒绝后不入库仍待独立验证。

按当时合同，检索绝对指标 Hit@5 `1.0`、MRR `0.944444` 达标，但 17 条 hard 子集从纯向量 `0.887255` 到完整链路 `0.931373`，提升仅 `0.044117647 < 0.15`，因此原报告为 `release_gates.passed=false`。用户当时只授权将该结果登记为兼容性限制并继续 Docker 收口；这段历史失败保留，后续合同修正见文末最新章节。

持久化续验通过 stdin 执行同一 canonical 只读快照。重启前后 SHA-256 均为 `761b76bb83958fe17989993c4d55b6111b7c9078a7a3f5b79e833ddec8351932`，7/51/51/51、9 conversations、28 messages、8 audits 及 7 项 API 复查逐字段一致，重启后认证 readiness 通过。官方前采样没有保存余额值，唯一后采样以脱敏 `RuntimeException` 失败，故官方余额差额不可得；用户接受流程前 `5 元`保守固定流程估算和现有 trace/耗时/零顶层重试证据作为授权边界。本文不声称实际费用低于 `20 元`，也不声称精确累计费用；已知历史 `0.113731 元`与此前一次未知部分费用继续分列。

最终卫生收口中，安全与可观测性回归 `14 passed`；源树 173 个文件、10 个 Docker/持久化 evidence 目录 128 个文件均扫描 0 findings。主项目已执行不带 `-v` 的 `docker compose down`，容器/网络移除，backend/frontend 镜像 `sha256:edbb25c0dd02b8b9a0086967d03087f0fb0a35752875a87da2d446f3e56a01b3`/`sha256:0228dd3b9d548ad43176fc8d0c095e78ca0d12aedb2cc24bfc52373ad2737b44` 与 `copilot-data`/`hf-cache` 两卷保留；克隆内单个临时 `backend/.env` 已删除，source/clone HEAD、tree、tracked status 和 `git diff --check` 无漂移。主流程、持久化、收口 evidence 分别为 ignored `20260814T114238Z-final-candidate-docker-deepseek-readiness-fixed/`、`20260814T123832Z-deepseek-persistence-resume/`、`20260814T131836Z-final-candidate-docker-closeout/`。由于 Compose frontend 浏览器交互与知识沉淀剩余语义未验证，active substage 停在“Docker 当前候选前端与知识沉淀补证授权等待”；hard-MRR、远端 CI、演示录屏、tag 与 Release 也未通过或未验证，最终状态保持“未验证/未发布”。

### 2026-08-14：Compose frontend 与知识沉淀定向补证

复用同一候选的 backend/frontend 镜像和 `copilot-data`/`hf-cache` 卷，以 `--no-build` 恢复 `18021/15191`。临时运行环境使用 `LLM_PROVIDER=ollama`、空 Qwen/DeepSeek 密钥，关闭 LLM 预热与外部启动探针，只允许本地 BGE-M3 embedding 和 BGE reranker。浏览器明确直连 `http://localhost:15191`，未启动宿主 Vite：无模型路由/权限 Playwright `3/3`、真实管理员知识审核 `1/1`、最终状态 trace `1/1`。

拒绝项状态为 `rejected`、`kb_document_id=null`，知识库、chunk、Qdrant vector 和 BM25 的拒绝前后 canonical SHA-256 均为 `821431ef4331fa7a8dfac8af4da9307f3f541104fac2da77b46ab5ba4d3c6f5b`。批准项生成文档 `e66007e3-e0c6-426f-9b3f-80470452f7a5` 和 3 chunks，文档 `7 -> 8`、vector/BM25 `51 -> 54`；关闭查询改写、保留 hybrid/RRF/真实 BGE rerank/默认阈值 `0.12` 的生产 Retriever 最终过滤命中该文档。

前后端 chat 请求、LLM warmup、DeepSeek/DashScope 标记均为 0；未配置 Qwen 时质量初筛降级为人工审核。安全与可观测性 `14 passed`，源树 173 个文件及本补证目录最终文本证据扫描均为 0 findings，逐文件 SHA-256 清单校验通过。证据位于 ignored `20260814T140355Z-final-candidate-docker-frontend-sedimentation/`。收尾已执行不带 `-v` 的 Compose `down`，容器/网络移除而镜像/卷保留，仅删除克隆内明确的临时 `.env`；source/clone HEAD 和 tree 仍为 `a65080ab`/`33f85ce3`，克隆 tracked 状态干净。Compose frontend 无模型交互与知识沉淀缺口解除，OPS-DEP-04 的浏览器知识问答仍因本轮禁止聊天端点而保持未验证；截至该子阶段，`hard_mrr_improvement=0.044117647 < 0.15` 仍按旧合同失败，active substage 当时转为“hard-MRR 发布门禁处置授权等待”。后续状态见下一节，项目始终保持“未验证/未发布”。

### 2026-08-14：hard-MRR 门禁合同修正与复验

本轮确认旧失败不是本地 `qwen2.5:7b` 导致：检索评测固定 `llm_client=None`、`enable_rewrite=False`，聊天模型没有参与。BGE-M3 纯向量 hard MRR `0.887254902` 加旧门槛 `0.15` 后目标为 `1.037254902`，超过 MRR 上限 `1.0`，剩余理论 headroom 仅 `0.112745098`。旧门槛来自弱向量基线，继续阻塞当前饱和基线在数学上不可满足。

门禁合同已修正为八项：完整链路 Hit@5 `>=0.85`、MRR `>=0.80`、hard MRR `>=0.90`、hard Hit@3 `=1.0`、Hit@5 不低于纯向量、hard MRR 不低于纯向量、Rerank hard MRR 严格高于 hybrid、`rerank_verified=true`。旧 `+0.15` 和实际 `+0.044117647` 仍保留为诊断，不伪装为达到旧阈值。新合同同时约束绝对质量、非回归、阶段增益和 fallback，因此是纠正不可达合同，不是放宽质量。

Python 3.11 门禁测试 `11 passed`、契约 `70 passed`、完整后端回归 `305 passed`。候选 Docker 镜像以空云端密钥、关闭 LLM 预热/查询改写/外部探针运行固定 39 条 eval 中的 30 条知识题、17 条 hard case 和 7 份原语料；输入 SHA-256 未漂移，仅调用本地 BGE-M3 与 BGE reranker。纯向量/hybrid/完整链路 MRR 为 `0.919444/0.927778/0.944444`，hard MRR 为 `0.887255/0.901961/0.931373`；完整链路 Hit@5、hard Hit@3 均 `1.0`，Rerank 相对 hybrid hard MRR 增益 `+0.029412`，30/30 verified、无 fallback，八项门禁全部通过。

ignored `20260814T144019Z-hard-mrr-gate-contract-bge/` 保存逐题三阶段排名、运行配置、原始日志、可解析 JSON、安全报告和 15 文件 SHA-256 清单。安全与可观测性 `14 passed`，源树/evidence 均 0 findings；无 chat、DeepSeek、Qwen 或付费模型请求，一次性容器已移除，候选镜像与 HF cache 卷保留，source/clone HEAD、tree 及克隆 tracked 状态未漂移。OPS-BLOCK-08 解除，active substage 推进到“Compose frontend 知识问答补证授权等待”；远端 CI、演示材料、tag 与 Release 仍未验证，最终状态保持“未验证/未发布”。

### 2026-08-14：Compose frontend 知识问答补证

签名候选 `a65080ab`、tree `33f85ce3` 复用既有 backend/frontend 镜像和两个持久化卷，以 `--no-build` 恢复项目 `copilot-final-deepseek-ready-114238`。临时环境只配置 DeepSeek V4 Pro，本地 BGE-M3 embedding 与 BGE reranker，Qwen 密钥为空，关闭 LLM 预热与外部启动探针，保留默认查询改写。Playwright 直接使用 Compose frontend `http://localhost:15191`，无宿主 Vite、无 `webServer`、单 worker、`retries=0`。

唯一问题“Pod 一直是 Pending 状态该怎么排查”以浏览器 `1/1` 通过，只有一次 `POST /api/v1/chat/stream`，没有非流式 chat、confirm、知识写入或工具调用。UI 返回非失败“知识问答”，SSE 正常结束并恢复发送按钮；trace 记录查询改写、hybrid、真实 Rerank、`rerank_fallback=false`、verified 生成和 5 个知识证据映射，9 个引用命中原始语料“Pod 停滞在 Pending 状态”。历史记录重开后问题、回答、trace 与引用保持一致，未产生第二次聊天请求。知识状态前后均为 8 文档、54 vectors、54 BM25，canonical SHA-256 均为 `4fd6407d4b9a59d273baf943e6a659955c0b48e788357ce8b56f4d5d6ca6426d`。

费用只登记授权上限 `1 元`、一个顶层请求和生产瞬时错误最多 3 次底层尝试的既有策略；没有可信余额差额，实际费用为 unknown。安全与可观测性 `14 passed`，source 173 文件、evidence 23 个可解析文本文件和 trace 压缩包专项扫描均 0 findings，27 文件 SHA-256 清单复算一致。证据位于 ignored `20260814T151334Z-final-candidate-compose-frontend-knowledge-qa/`。Compose 收尾 `down` 未带 `-v`，镜像/卷保留，克隆临时 `.env` 已单文件删除，source/clone 身份和 tracked 状态无漂移。

本轮只补齐 Compose 前端知识问答交互，没有重新执行固定 39 条生成评测，也没有改变 hard-MRR 合同或历史 `0.044117647`。`OPS-DEP-04`、`OPS-BIZ-02`、本地完整链路和 Docker 本地验收通过；active substage 推进到“GitHub 发布准备授权等待”。远端 CI、演示材料、tag 与 Release 仍未验证，最终状态保持“未验证/未发布”。

### 2026-08-15：v1.0.0 本地演示材料验收

发布演示在候选 `a65080ab` 的独立 `--no-hardlinks` 克隆中使用空云端密钥、本地 `qwen2.5:7b`、`bge-m3` 和 BGE Reranker。最多两次顶层问答的授权已用完：第一次安全降级为信息不足，第二次成功完成 Pending 知识问答并展示原始语料引用、Trace 和历史记录；没有 DeepSeek/DashScope 请求，也没有为演示调整实现、语料或评测集。

`docs/assets/demo.gif` 为 `1280x720`、约 27 秒、177255 bytes，登录、提问、生成、回答、引用、Trace 和历史页面完整可见，画面专项检查未发现密码、JWT、密钥或个人信息。ignored `20260815T-v1-release-closeout/` 保留两次结果、原始截图和生成脚本；Compose 已不带 `-v` 收尾，独立克隆临时 `.env` 已单文件删除。`OPS-GH-07` 通过，但远端 CI、签名 tag 和 GitHub Release 尚未验证，因此最终结论仍为“未验证/未发布”。

发布准备变更随后在 Python 3.11.15 下通过全量 `305 passed`、契约 `70 passed` 和 hard-MRR 门禁 `11 passed`；前端 typecheck/build、npm audit（0 vulnerabilities）、Compose 配置与 `git diff --check` 通过。安全与可观测性脚本扫描 source 173 个文件和本轮 evidence 3041 个文件均 0 findings；Gitleaks 最终暂存内容 0 findings，完整历史 5 个命中均为生成示例 confirmation token 或单测专用 JWT secret，没有真实密钥。该结果允许进入公开仓库与远端 CI 阶段，但不提前把远端门禁登记为通过。

公开仓库 [qianlin-code/k8s-devops-copilot](https://github.com/qianlin-code/k8s-devops-copilot) 已创建，默认分支为 `main`、MIT License 已识别。发布准备提交 `c65b271` 使用 GitHub noreply 邮箱和既有 Ed25519 SSH 签名；首轮 [GitHub Actions run 31821164169](https://github.com/qianlin-code/k8s-devops-copilot/actions/runs/31821164169) 的 Python 3.11 后端与 Node 20 前端任务均通过，workflow 无需修改。`OPS-GH-06` 通过；`OPS-GH-10` 仍等待文档提交 CI、签名 tag、tag CI 和 [v1.0.0 Release](https://github.com/qianlin-code/k8s-devops-copilot/releases/tag/v1.0.0) 实际创建，完成前最终结论保持“未验证/未发布”。
